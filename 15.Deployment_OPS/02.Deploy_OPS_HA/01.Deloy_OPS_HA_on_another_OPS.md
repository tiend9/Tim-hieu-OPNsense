# DEPLOY OPENSTACK HA 3 NODE CONTROLLER ON OPENSTACK USE CEPH RDB BACKEND

## I. ĐẶC ĐIỂM CỦA OPSTACK HA

### 1. Đặc điểm của HA trong OPS

**HA (High Availability)** = Tính sẵn sàng cao

Mục tiêu của HA với OPS:

#### **Đảm bảo không downtime (hoặc downtime tối thiểu)**

- Khi một node (controller, network, compute…) bị lỗi → hệ thống vẫn chạy

- Người dùng vẫn:

  - Tạo VM
  - Truy cập VM
  - Sử dụng dịch vụ bình thường

=> Ví dụ: `controller-1` chết → `controller-2/3` tự động tiếp quản

#### **Tự động failover (chuyển đổi khi có lỗi)**

- HA giúp:

  - Phát hiện node/service bị lỗi
  - Tự động chuyển sang node khác (failover)
  - Không cần can thiệp thủ công

=> Thường dùng:

- `Pacemaker` / `Corosync`
- `HAProxy` + `Keepalived` (VIP)

#### **Cân bằng tải (Load Balancing)**

- Nhiều node controller cùng xử lý request

- Tránh quá tải 1 node

=> Ví dụ:

- `API` (Nova, Keystone, Neutron) được load balance qua `HAProxy`

#### **Tăng độ tin cậy (Reliability)**

- Tránh single point of failure (SPOF)

Các thành phần cần HA:

- Controller nodes (`API`, scheduler…)
- Database (`MariaDB`; `Galera` cluster)
- Message Queue (`RabbitMQ` cluster)
- Network services (`L3 agent`, `DHCP`agent…)

#### Bảo vệ dữ liệu & trạng thái hệ thống

DB cluster đảm bảo:

- Không mất dữ liệu khi 1 node chết
- Dữ liệu được replicate giữa các node

#### Đảm bảo SLA (Service Level Agreement)

HA giúp hệ thống:

- Đạt uptime cao (99.9% → 99.99%)
- Phù hợp môi trường production

## II. MÔ HÌNH DEPLOY OPENSTACK HA

### 1. Mô hình cài đặt

Mô hình cài đặt OpenStack Yoga trên môi trường OPS lab có sẵn:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_01.png)

### 2. IP planning

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_02.png)

### 3. Yêu cầu Từ OpenStack hạ tầng (Cụm bên ngoài )

Trước khi tạo VM cho cụm HA phải đảm bảo:

#### 3.1 Nested Virtualization

Trên compute node của OpenStack hạ tầng:

```bash
cat /sys/module/kvm_intel/parameters/nested

# Phải = `Y`
```

Nếu không → cụm HA bên trong sẽ không tạo được VM.

#### 3.2 Flavor cho VM của cụm HA

Tất cả VM controller/compute/storage phải:

- CPU mode: `host-passthrough`
- Expose `VMX`/`SVM`
- RAM đủ lớn
- Disk nên dùng volume (không dùng `ephemeral`)

#### 3.3 Network yêu cầu

- Cho phép `promiscuous mode`
- **Tắt port security** hoặc cấu hình `allowed-address-pairs`
- Hỗ trợ `Trunk` (nếu dùng VLAN)
- Kiểm tra `MTU` (khuyên dùng 1450 cho tenant network)

### 4. Yêu cầu hạ tầng chung trên các node & Service cần có trên từng node

#### 4.1 Cấu hình chung trên tất cả các node (Controller/Compute/Storage)

**OS**:

- Ubuntu 24.04 LTS (khuyến nghị)
- Cấu hình static IP
- Hostname chuẩn
- `/etc/hosts` đầy đủ toàn cluster

Ví dụ:

```bash
192.168.70.81  controller 1
192.162.70.83  controller 2
192.162.70.85  controller 3
```

**System config** bắt buộc:

- Disable swap
- Enable NTP (chrony)
- `net.ipv4.ip_forward = 1` (compute + network)
- `net.bridge.bridge-nf-call-iptables` = 1
- Tắt firewall hoặc mở port đầy đủ
- Cài gói cơ bản: `curl`, `wget`, `vim`, `net-tools`

**Network** theo layout:

| Network                            | CIDR              | Dùng cho             |
| -----------------------------------| ----------------- | -------------------- |
| MGMT + API                         | `10.10.10.0`/24   | Service nội bộ       |
| Tenant                             | `10.10.20.0`/24   | VXLAN                |
| Ceph Public/Client Network         | `10.10.30.0`/24   | Storage backend      |
| Ceph Cluster/Replica Network       | `10.10.30.0`/24   | Storage backend      |
| Provider Network                   | `192.168.70.0`/24 | External Net + VIP   |

#### 4.1 Controller Node

Mỗi controller sẽ chạy đầy đủ **Control Plane Service.**

Gồm:

- `HA Layer`

  - HA Proxy
  - Keepalived (VIP: `10.10.10.100`)

- `Database Layer`

  - MariaDB Galera Cluster (3 Node)
  - Port:

    - `3306`
    - `4567`
    - `4568`
    - `4444`

- `Message Queue`

  - `RabbitMQ cluster` (3node)
  - Port:

    - `5672`
    - `25672`

- `Identity`

  - `Keystone`

- `Image`

  - `Glance` (backend Ceph RBD)

- `Compute Control`

  - `Nova API`
  - `Nova Scheduler`
  - `Nova Conductor`
  - `Placement API`

- Cache

  - `Memcached`

- `Networking Control`

  - `Neutron Server`
  - `ML2 plugin`
  - `DHCP agent`
  - `L3 agent` (nếu không dùng full DVR)

- `Block Storage control`

  - `Cinder API`
  - `Cinder Scheduler`
  - `Cinder Volume` (nếu dùng Ceph thì volume có thể ở storage node)

- `Dashboard`

  - Horizon

#### 4.2 Compute Node (x2 -DVR)

- Service cần có

  - `nova-compute`
  - `neutron-openvswitch-agent` (hoặc linuxbridge)
  - `neutron-l3-agent` (DVR mode)
  - `neutron-metadata-agent`
  - Virtualization

    - KVM
    - libvirt

Kiểm tra:

```bash
egrep -c '(vmx|svm)' /proc/cpuinfo
```

- Cần cấu hình Kernel (Kernel Config)

```bash
net.ipv4.ip_forward=1
net.bridge.bridge-nf-call-iptables=1
```

#### 4.3 Storage Node (x3 - Ceph)

**Chuẩn bị Disk**:

Mỗi OSD:

- 1 volume riêng (không dùng disk OS)
- Khuyến nghị raw volume

**Ceph service**:

- `ceph-mon` (3 node)
- `ceph-mgr`
- `ceph-osd`
- `rbd`

**Pool cần tạo**:

- `images`
- `volumes`
- `backups`
- `vms`

**Nếu chạy Cinder Volume tại đây**:

- `Cinder-volume`(RBD backend)

#### 4.4 Service Logic tổng thể

VIP (`10.10.10.100`) publish:

- `5000` -> `Keystone`
- `8874` -> `Nova`
- `9292` -> `Glance`
- `9696` -> `Neutron`
- `8776` -> `Cinder`
- `80`   -> `Horizon`

### 5. Những khó khăn & lưu ý khi cài ManualInstall (so với các cách khác Automation or use Kolla-Ansible)

#### 5.1 HA manual config rất phức tạp

- Sai 1 dòng cấu hình `Galera` → **cluster split-brain**
- Sai cấu hình `Rabbit` → **message queue** không sync
- Sai endpoint → service không register

=> Manual = Rất dễ lệch config giữa các controller

Automation đảm bảo:

- File cấu hình giống nhau
- Restart theo đúng thứ tự
- Sync database migration đúng cách

=> Bản chất của Automation đã chạy các file bash scripts theo thứ tự

#### 5.2 Nested tăng độ khó

Ta đang chạy: OpenStack trên 1 OpenStack hạ tầng khác.

- Nested `KVM`
- Nested `VXLAN`
- Nested routing
- Nested security group

**Manual** cực dễ vỡ ở:

- `MTU` sai
- `iptables rule` xung đột
- `Bridge` mapping sai
- `DVR` không hoạt động do port security

Automation (Kolla/OpenStack-Ansible) xử lý:

- `Bridge` mapping tự động
- `ML2` config chuẩn
- `Sync neutron agent` đồng bộ

#### 5.3 HA yêu cầu cấu hình đồng nhất 100%

Ví dụ:

`nova.conf` trên 3 controller phải giống nhau 99%.

Chỉ cần khác:

```bash
transport_url
memcached_servers
connection
```

→ Service không sync.

=> `Manual` rất dễ sai.
=> `Automation` dùng template → đảm bảo đồng nhất.

#### 5.4 Database migration cực nguy hiểm khi manual

Trong HA:

Chỉ 1 node được chạy:

```bash
nova-manage db sync
```

Nếu **chạy nhầm nhiều node** → có thể **corrupt schema**.

`Automation` kiểm soát:

- Node nào chạy migration
- Chạy đúng thứ tự
- Lock đúng service

#### 5.5 Khi lỗi xảy ra, manual rất khó rollback

Ví dụ:

- `Galera` mất quorum
- `RabbitMQ` mất cluster
- `Keystone` token lỗi
- `Neutron` DVR không route

-> `Manual` sửa = mò từng node.

-> `Automation`: `Re-run playbook` là `self-heal`.

#### 5.6 NGUYÊN TẮC KHI CÀI MANUAL HA

- Chỉ chạy DB migration trên 1 controller duy nhất
- Mọi file config giữa các controller phải giống nhau 100%
- Tất cả endpoint phải trỏ về `VIP`
- `Ceph` phải chạy ổn định trước khi cài `Glance`/`Cinder`
- `Nested KVM` phải `OK` trước khi cài `nova-compute`.
- Test từng `PHASE` một khi được mới sang `PHASE` tiếp

### 6. Lưu ý

```text
- Tất cả VM node dùng Ubuntu24.04
- Đăng nhập với quyền `root` cho tất cả các bước cài đặt
- Các thao tác sửa file trong hướng dẫn này sử dụng lệnh nano hoặc vim
- Password thống nhất cho tất cả các dịch vụ là `Tien@123` or `Tien123`
- Snapshot ngay sau PHASE 0
```

## III . DEMO CÁC BƯỚC

### 1. `PHASE 0` – Chuẩn bị nền (TẤT CẢ NODE)

- Static IP
- `/etc/hosts` đầy đủ
- Disable swap
- **Chrony** đồng bộ giờ
- `net.ipv4.ip_forward=1` (compute)
- `Bridge-nf-call-iptables`=1
- Nested KVM kiểm tra `OK`

### 2. `PHASE 1` – HA Infrastructure Layer

#### MariaDB Galera Cluster (controller1-3)

- Cài Galera trên cả 3 node

- Bootstrap cluster từ controller1

Kiểm tra:

```bash
SHOW STATUS LIKE 'wsrep_cluster_size';
#Phải = 3
```

=> nếu sai -> dừng lại sửa, không cài tiếp.

#### RabbitMQ Cluster (controller1-3)

- Join cluster đầy đủ

- Sync user openstack

Kiểm tra:

```bash
rabbitmqctl cluster_status
```

#### Memcached (controller1-3)

Đảm bảo listen đúng IP management.

#### HAProxy + Keepalived

VIP: `10.10.10.100`

Test:

```bash
ip a | grep 10.10.10.100
```

-> Failover thử bằng cách tắt 1 controller.

### 3. `PHASE 2` – Keystone (chỉ cài đầy đủ trên controller1 trước)

- Tạo DB keystone

- Cấu hình `keystone.conf` (trỏ DB về Galera)

Chạy:

```bash
keystone-manage db_sync
```

`Lưu ý`: Chỉ chạy trên controller1

- Bootstrap

- Tạo endpoint trỏ về VIP

- Sau khi `OK` → copy config sang controller2-3 → start service.

### 4. `PHASE 3` – Glance

1. Tạo DB

2. Cấu hình backend Ceph RBD

3. `db_sync` (chỉ controller1)

4. Start service cả 3 node

=> Test upload image.

### 5. `PHASE 4` – Placement

- Tạo `DB` → `db_sync` → register endpoint → test.

### 6. `PHASE 5` – Nova

#### Controller node

- `nova-api`
- `nova-scheduler`
- `nova-conductor`

Tạo DB:

- `nova_api`
- `nova`
- `nova_cell0`

Chạy:

```bash
nova-manage api_db sync
nova-manage cell_v2 map_cell0
nova-manage db sync
nova-manage cell_v2 create_cell
```

`Lưu ý`: Chỉ `controller1` chạy lệnh manage.

**Compute Node.**

- cài `nova-compute`
- cấu hình `transport_url` (`Rabbit cluster`)
- cấu hình `placement endpoint` (`VIP`)

Kiểm tra:

```bash
openstack compute service list
```

### 7. `PHASE 6` – Neutron (DVR mode)

Controller:

- `neutron-server`

Compute:

`neutron-openvswitch-agent`
`neutron-l3-agent (DVR)`
`neutron-metadata-agent`

`Nested` chú ý:

- MTU 1450 (Tenant NetWork)
- MTU 1500 (API + MNGT Network)
- MTU 9000 (Ceph Network) nhưng không biết hạ tầng dưới có hỗ trợ không thì để 1450 vậy nhưng sẽ rất chậm
- Port security nếu dùng provider

#### 8. `PHASE 7` – Ceph + Cinder

Ceph phải healthy:

```bash
ceph -s
```

Tạo pool:

images
volumes
backups
vms

Cinder:

controller: `cinder-api` + scheduler  
storage: `cinder-volume` (RBD backend)

#### 9. `PHASE 8` – Horizon

Cài trên controller
Trỏ endpoint về `VIP`

## IV. DEPLOY

`LUƯ Ý QUAN TRỌNG`: LÀM ĐÚNG THEO THỨ TỰ CÁC BƯỚC KHÔNG ĐƯỢC THIẾU VÀ CŨNG NHƯ CHECK ĐÚNG SYNTAX, ĐÚNG VERSION.

### `Bước 1`<`PHASE0`>: Tạo Volume + Network trên OpenStack hạ tầng

`Đối với tạo Volume`:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_03.png)

`Đối với tạo Network`:

- `Tạo dải mạng Tenant Network`: (Nhớ `MTU`=1450):

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_04.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_05.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_06.png)

- `Tạo dải mạng Ceph Public/Client Network`:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_07.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_08.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_09.png)

- `Tạo dải mạng API + MNGT Network`:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_10.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_11.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_12.png)

- `Tạo dải mạng Ceph Cluster Network`:

Sau khi hoàn thành ta có 4 dải mạng sau:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_13.png)

`Đối với tạo Ports`:

- Vào Network rồi nhấp chuột trái vào dải mình muốn tạo Ports:(Nhớ add `Security Group`hoặc group Network mình tự tạo cho cụm)

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_15.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_16.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_17.png)

- Sau hoàn thành ta có các Ports được cấu hình:(Nhớ cấu hình chuẩn)

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_18.png)

=> Ta tạo Port với các dải còn lại (Nhớ tắt `Port Security`)

`Đối với tạo Instances`:(StorageNode nhớ dùng tool `Partition manual`, còn các Node khác dùng `Automatic`)

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_14.png)

=> Nếu thấy thừa IP có thể Detach Interface

`Đối với tạo Rules trên Security Group`- Các VM trong Project được gán vào Group `DEFAULT` lên ta Add Rule như sau:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_19.png)

### `Bước 2`: Cấu hình nền trên tất cả các Node (Controller,Compute,Storage Node)

#### 1. Cấu hình Interface

##### a. Trên Cotroller Node

Ta xoá đường route gateway `10.10.10.1` để có thể ssh vào được từ MobaXTerm:

```bash
ip route del default via 10.10.10.1 # Trên 3 controller node là SSH đc
```

Vào file `/etc/netplan`. Cấu hình **Ip static** cho từng interface trên từng Node Controller:

- Trên Controller 1:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0 đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

  GNU nano 7.2                                                       /etc/netplan/50-cloud-init.yaml *
network:
  version: 2
  ethernets:
    eth2:
      match:
        macaddress: "fa:16:3e:08:4f:e4"
      dhcp4: no
      addresses:
        - "192.168.70.72/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      nameservers:
        addresses: [8.8.8.8]
      set-name: "eth2"
    eth3:
      match:
        macaddress: "fa:16:3e:f2:f7:5f"
      addresses:
      - "10.10.10.11/24"
      set-name: "eth3"
      mtu: 1450
    eth0:
      match:
        macaddress: "fa:16:3e:5f:af:fc"
      addresses:
      - "10.10.30.34/24"
      set-name: "eth0"
      mtu: 1450      

# Apply cấu hình

sudo netplan apply

```

- Trên Controller 2:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0 sai IP đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

network:
  version: 2
  ethernets:
    eth2:
      match:
        macaddress: "fa:16:3e:22:86:10"
      dhcp4: no
      addresses:
        - "192.168.70.74/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      set-name: "eth2"
      nameservers:
        addresses: [8.8.8.8]
    eth3:
      match:
        macaddress: "fa:16:3e:d8:5b:66"
      addresses:
      - "10.10.10.12/24"
      set-name: "eth3"
      mtu: 1450
    eth0:
      match:
        macaddress: "fa:16:3e:ef:d4:da"
      addresses:
      - "10.10.30.35/24"
      set-name: "eth0"
      mtu: 1450    
# Apply cấu hình

sudo netplan apply
```

- Trên Controller 3:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0 sai IP đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

network:
  version: 2
  ethernets:
    eth2:
      match:
        macaddress: "fa:16:3e:b6:4b:46"
      dhcp4: no
      addresses:
        - "192.168.70.76/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      set-name: "eth2"
      nameservers:
        addresses: [8.8.8.8]
    eth3:
      match:
        macaddress: "fa:16:3e:24:5d:fe"
      addresses:
      - "10.10.10.13/24"
      set-name: "eth3"
      mtu: 1450
    eth0:
      match:
        macaddress: "fa:16:3e:92:08:e9"
      addresses:
      - "10.10.30.36/24"
      set-name: "eth0"
      mtu: 1450

# Apply cấu hình

sudo netplan apply
```

`Lưu ý`: Địa chỉ `MAC` phải trùng khớp với Interface

##### b. Trên ComputeNode

Ta xoá đường route gateway `10.10.10.1` để có thể ssh vào được:

```bash
ip route del default via 10.10.10.1 # Trên 2 compute node là SSH đc
```

- Trên Compute Node 1:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0,2 sai IP đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

network:
  version: 2
  ethernets:
    eth3:
      match:
        macaddress: "fa:16:3e:72:c2:59"
      dhcp4: no
      addresses:
        - "192.168.70.78/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      set-name: "eth3"
      nameservers:
        addresses: [8.8.8.8]
    eth4:
      match:
        macaddress: "fa:16:3e:ee:e9:21"
      addresses:
      - "10.10.20.21/24"
      set-name: "eth4"
      mtu: 1450
    eth5:
      match:
        macaddress: "fa:16:3e:38:b3:00"
      addresses:
      - "10.10.10.14/24"
      set-name: "eth5"
      mtu: 1450
    eth6:
      match:
        macaddress: "fa:16:3e:c7:c4:d9"
      addresses:
      - "10.10.30.37/24"
      set-name: "eth6"
      mtu: 1450
# Apply cấu hình

sudo netplan apply
```

- Trên Compute Node 2:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0,2 sai IP đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

network:
  version: 2
  ethernets:
    eth3:
      match:
        macaddress: "fa:16:3e:16:b1:45"
      dhcp4: no
      addresses:
        - "192.168.70.80/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      set-name: "eth3"
      nameservers:
        addresses: [8.8.8.8]
    eth4:
      match:
        macaddress: "fa:16:3e:6d:f9:28"
      addresses:
      - "10.10.20.22/24"
      set-name: "eth4"
      mtu: 1450
    eth5:
      match:
        macaddress: "fa:16:3e:d7:c2:83"
      addresses:
      - "10.10.10.15/24"
      set-name: "eth5"
      mtu: 1450
    eth6:
      match:
        macaddress: "fa:16:3e:00:05:b6"
      addresses:
      - "10.10.30.38/24"
      set-name: "eth6"
      mtu: 1450

# Apply cấu hình

sudo netplan apply
```

##### c. Trên Storage Node

Ta xoá đường route gateway `10.10.10.1` để có thể ssh vào được:

```bash
ip route del default via 10.10.10.1 # Trên 3 storage node là SSH đc
```

- Trên Storage Node 1:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0,2 sai IP đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

network:
  version: 2
  ethernets:
    eth3:
      match:
        macaddress: "fa:16:3e:8a:89:22"
      dhcp4: no
      addresses:
        - "192.168.70.82/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      set-name: "eth3"
      nameservers:
        addresses: [8.8.8.8]
    eth4:
      match:
        macaddress: "fa:16:3e:0c:9a:20"
      addresses:
      - "10.10.10.16/24"
      set-name: "eth4"
      mtu: 1450
    eth5:
      match:
        macaddress: "fa:16:3e:14:85:ff"
      addresses:
      - "10.10.30.31/24"
      set-name: "eth5"
      mtu: 1450
    eth6:
      match:
        macaddress: "fa:16:3e:eb:cf:a1"
      addresses:
      - "10.10.40.41/24"
      set-name: "eth6"
      mtu: 1450

# Apply cấu hình

sudo netplan apply
```

- Trên Storage Node 2:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0,2 sai IP đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

network:
  version: 2
  ethernets:
    eth3:
      match:
        macaddress: "fa:16:3e:07:06:cc"
      dhcp4: no
      addresses:
        - "192.168.70.84/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      set-name: "eth3"
      nameservers:
        addresses: [8.8.8.8]
    eth4:
      match:
        macaddress: "fa:16:3e:e9:ca:76"
      addresses:
      - "10.10.10.17/24"
      set-name: "eth4"
      mtu: 1450
    eth5:
      match:
        macaddress: "fa:16:3e:25:0d:8a"
      addresses:
      - "10.10.30.32/24"
      set-name: "eth5"
      mtu: 1450
    eth6:
      match:
        macaddress: "fa:16:3e:36:7e:90"
      addresses:
      - "10.10.40.42/24"
      set-name: "eth6"
      mtu: 1450

# Apply cấu hình

sudo netplan apply
```

Trên Storage Node 3:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0,2 sai IP đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

network:
  version: 2
  ethernets:
    eth3:
      match:
        macaddress: "fa:16:3e:c9:f1:22"
      dhcp4: no
      addresses:
        - "192.168.70.86/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      set-name: "eth3"
      nameservers:
        addresses: [8.8.8.8]
    eth4:
      match:
        macaddress: "fa:16:3e:65:5f:6c"
      addresses:
      - "10.10.10.18/24"
      set-name: "eth4"
      mtu: 1450
    eth5:
      match:
        macaddress: "fa:16:3e:d8:cc:38"
      addresses:
      - "10.10.30.33/24"
      set-name: "eth5"
      mtu: 1450
    eth6:
      match:
        macaddress: "fa:16:3e:8f:94:a4"
      addresses:
      - "10.10.40.43/24"
      set-name: "eth6"
      mtu: 1450
# Apply cấu hình

sudo netplan apply
```

##### d. Kiểm tra

Trên cả 3 Node tiến hành ping ra ngoài mạng `google.com` và ra Gateway `192.168.70.1`

```bash
ping -c 4 192.168.70.1
ping -c 4 google.com
```

=> Đã kiểm tra trên lab các Node `OK` -> Chuyển bước tiếp

`Ví dụ`: Check trên Node Controller 1

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_20.png)

#### 2. Cấu hình hostname

Cấu hình đủ 2 config sau:

##### a. Cấu hình hostnamectl

Ví dụ - Trên Controller 1:

```bash
hostnamectl set-hostname controller1
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_21.png)

Làm tương tự với:

```text
controller1
controller2
controller3
compute1
compute2
storage1
storage2
storage3
```

##### b. Cấu hình file `/etc/hosts`

Cấu hình trên tất cả các Node:

```bash
# Chèn
10.10.10.11 controller1
10.10.10.12 controller2
10.10.10.13 controller3
10.10.10.14 compute1
10.10.10.15 compute2
10.10.10.16 storage1
10.10.10.17 storage2
10.10.10.18 storage3
10.10.10.100 vip
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_22.png)

##### c. Disable Swap

Cấu hình trên tất cả các Node:

```bash
swapoff -a
sed -i '/ swap / s/^/#/' /etc/fstab
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_23.png)

##### d. Cài gói cơ bản và Chorny

Cấu hình trên tất cả các Node:

```bash
apt update
apt install -y chrony vim curl wget net-tools
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_24.png)

##### e. Enable IP forward

Cấu hình trên 2 Node Compute + Controller Node:

```bash
echo "net.ipv4.ip_forward=1" >> /etc/sysctl.conf
echo "net.bridge.bridge-nf-call-iptables=1" >> /etc/sysctl.conf
modprobe br_netfilter
echo br_netfilter >> /etc/modules
sysctl -p
```

Trong đó:

- `net.ipv4.ip_forward=1`: Nó cho phép Linux forward packet giữa các interface.
- `net.bridge.bridge-nf-call-iptables=1`: Cho phép traffic đi qua bridge (OVS/Linux bridge) vẫn bị `iptables` xử lý.

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_25.png)

##### f. Kiểm tra Nested KVM

Check trên Compute Node:

```bash
egrep -c '(vmx|svm)' /proc/cpuinfo
```

=> Nếu kết quả `>0` là `OKE`.(Số càng to càng tốt nhé là số core hỗ trợ ảo hoá)

##### g. Tắt Firewall

Trên tất cả các node cấu hình:

```bash
systemctl disable ufw
systemctl stop ufw
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_26.png)

### `Bước 3`: Snapshot Volume để tiện Rollback khi có sự cố

#### 1. Tắt tất cả VM trên OpenStack hạ tầng

Nên shutdown VM trước để sạch:

```bash
shutdown -h now
```

Hoặc từ Horizon: `Shutdown instance`.

#### 2. Create Snapshot Volume

Do VM trong Bài lab ta boot from Volume lên ta sẽ `snapshot` volume là chuẩn nhất (`snapshot` VM nếu VM boot từ ephemeral disk)

Và do dung lượng Volume trên hạ tầng OpenStack có hạn lên ta sẽ snapshot 3 Controller Node vì đang cần cấu hình DB và Rabbit MQ chủ yếu config controller thui

Vào `Projects`->`Volumes`->`Volumes`->`Create SnapShot`, ta sẽ được 3 Volume SnapShot:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_27.png)

Khi cần Rollback ta chỉ cần:

`Bước 1`: Stop VM

```bash
Project → Compute → Instances → Stop Instance
```

`Bước 2`: Vào Volume Snapshot

```bash
Project → Volumes → Snapshots
```

Chọn `snapshot` → **Create Volume**

`Bước 3`: Detach volume cũ

```bash
Project → Volumes → Volumes
```

→ Detach Volume khỏi instance

`Bước 4`: Attach volume mới (tạo từ snapshot)

Attach volume mới vào VM.

`Bước 5`: Start lại VM

### `Bước 4`:<`PHASE1`>Cài và Config Maria DB + Galera HA

Trên cả 3 Node Controller:

```bash
apt update
apt install mariadb-server galera-4 -y
```

Check:

```bash
systemctl status mariadb.service
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_28.png)

Tắt service trước khi config trên 3 controller:

```bash
systemctl stop mariadb
```

Cấu hình Galera:

- Trên cả 3 Controller chỉnh file:

```bash
nano /etc/mysql/mariadb.conf.d/60-galera.cnf

# Dán config

[mysqld]
bind-address=10.10.10.11
binlog_format=row
default_storage_engine=InnoDB
innodb_autoinc_lock_mode=2

# Quan trọng (khai báo wsrep_provider)
wsrep_provider=/usr/lib/galera/libgalera_smm.so

[galera]
wsrep_on=ON
wsrep_cluster_name="openstack_cluster"
wsrep_cluster_address="gcomm://10.10.10.11,10.10.10.12,10.10.10.13"

wsrep_node_name="controller1"
wsrep_node_address="10.10.10.11"

# Đối với controller 2. Thay 3 dòng

bind-address=10.10.10.12
wsrep_node_name="controller2"
wsrep_node_address="10.10.10.12"

# Đối với controller 3. Thay 3 dòng

bind-address=10.10.10.13
wsrep_node_name="controller3"
wsrep_node_address="10.10.10.13"
```

Tiếp theo, ta sẽ thử Bootstrap cluster trên Controller 1

```bash
galera_new_cluster
```

Check:

```bash
mysql -u root -e "SHOW STATUS LIKE 'wsrep_cluster_size';"
mysql -u root -e "SHOW STATUS LIKE 'wsrep_cluster_status';"
```

=> Ra `1` và `Primary` là Node Controller 1 đã Bootstrap thành công

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_29.png)

Giờ để có mô hình Galera 3 Node HA thành công ta cần phải Boot tiếp Cluster Galera từ 2 Node còn lại :

- Khởi tạo Service trên Controller 2:

```bash
systemctl start mariadb
```

- Check trên Controller 1:

```bash
mysql -u root -e "SHOW STATUS LIKE 'wsrep_cluster_size';"
```

=> Ra `2` là thành công tạo thêm cluster mới

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_30.png)

- Khởi tạo Service trên con Controller 3:

```bash
systemctl start mariadb
```

- Check trên Controller 1:

```bash
mysql -u root -e "SHOW STATUS LIKE 'wsrep_cluster_size';"
```

=> Ra `3` là thành công tạo thêm cluster mới

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_31.png)

Như vậy ta đã có database HA 3 node. Giờ chỉ còn Set passwork remote access với user `Root`.

- Trên 1 Controller bất kì:(Controller 1)

```bash
mysql_secure_installation

# Sau đó nó sẽ hỏi:

1. Có muốn set pass cho tài khoản root ? <Answer:Tuỳ>
2. Có muốn set xác thực lớp unix_socket ? <Answer:Tuỳ>
3. Có muốn bỏ user ẩn danh ? <Answer:Có>
4. Có cấm truy cập remote qua mạng bằng user root không? <Answer:Ko>
5. Có muốn bỏ database test không ? <Answer:Có>
6. Có muốn Reload bảng quyền ngay lập tức không? <Answer:Có>

# Sau đó đăng nhập vào mysql

mysql -u root -p

# Tạo user root remote accesss(tk root truy cập DB từ xa, bỏ nếu ae set ở trên rồi!)

CREATE USER 'root'@'%' IDENTIFIED BY 'Tien@123';
GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' WITH GRANT OPTION;
FLUSH PRIVILEGES;

# Tạo user riêng cho haproxy accesss vào DB (Không set mk để không cần set quyền cho HAProxy - Production thật thì lên set pass và sẽ set quyền cho HAProxy)

CREATE USER 'haproxy'@'%' IDENTIFIED BY '';
GRANT USAGE ON *.* TO 'haproxy'@'%';
FLUSH PRIVILEGES;

# Check

SELECT user, host FROM mysql.user;
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_32.png)

Test login Remote từ Controller khác:(Controller 2)

```bash
mysql -u root -p -h 10.10.10.11

# -u: Đăng nhập với user root
# -p: Yêu cầu nhập Password
# -h: Kết nối MySQL ở IP 10.10.10.11(Nếu không nó sẽ kết nối tới localhost -> Dùng socket nội bộ)
```

Kết quả:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_33.png)

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_34.png)

Test lại Galera còn Primary và Size bằng 3 không(Trên cả 3 controller):

```bash
SHOW STATUS LIKE 'wsrep_cluster_status';
SHOW STATUS LIKE 'wsrep_cluster_size';
SHOW STATUS LIKE 'wsrep_ready';
SHOW STATUS LIKE 'wsrep_connected';
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_35.png)

`LƯU Ý QUAN TRỌNG`: **Không được** `Shutdown` **các VM cùng 1 lúc**. Sau khi `Shutdown` or `Restart` lại các Controller thì ta lên check trước 3 Node Controller:

```bash
cat /var/lib/mysql/grastate.dat

# Nếu Node nào hiện

safe_to_bootstrap: 1

# Thì phải khởi động Node đó đầu tiên bằng

galera_new_cluster

# Không cả 3 cluster sẽ chết 

# Bản chất Galera chỉ đặt safe_to_bootstrap: 1 cho node cuối cùng shutdown đúng cách.

# Nếu Cluster đang healthy or restart từng node một thì safe_to_bootstrap: 0 => Thì OK không cần dùng lệnh trên

# Sau đó restart mariaDB ở 2 node còn lại
systemctl start mariadb
```

Do cấu hình Galera HA đã ổn lên ta sẽ snapshot volume hiện tại và xoá volume trước đó đồng thời test `safe_to_bootstrap: 1` luôn

- Tắt hết 3 Controller VM và xoá Volume snapshot cũ và Add một cái mới:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_36.png)

- Khởi động lại và ta sẽ Check xem Node Nào sẽ phải Bootstrap lại đầu tiên:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_37.png)

=> Ta thấy ở đây Node Controller 3 đang có `safe_to_bootstrap: 1` lên ta phải bootstrap lại từ nó trước

- Check lại `wsrep_cluster_status` và `wsrep_cluster_size`:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_38.png)

=> Size `=1` Start lại MariaDB service trên 2 Node còn lại:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_39.png)

=> Size `=3` Là Oke và giờ thằng `10.10.10.13` là thằng primary DB, nhớ remote access DB vào nó !

### `Bước 5`: Cài và Config Rabbit MQ Cluster (Message Queue HA)

Đầu tiên, cài Rabbit MQ trên cả 3 Controller:

```bash
apt update
apt install rabbitmq-server -y
```

Sau đó sửa config:

```bash
# Vào

nano /etc/rabbitmq/rabbitmq.conf

# Thêm

listeners.tcp.default = 10.10.10.11:5672

# Tránh sau này confilct IP với HA Proxy
# Thêm tương tự với các Node khác nhưng phải thay bằng IP Node đó
```

Enable service:

```bash
systemctl enable rabbitmq-server
systemctl start rabbitmq-server
```

Check:

```bash
systemctl status rabbitmq-server.service
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_40.png)

Sau đó, đồng bộ **Erlang Cookie**

- Trên Controller 1:

```bash
cat /var/lib/rabbitmq/.erlang.cookie
#Copy nội dung hiện ra
```

- Trên Controller Node 2 + 3:

```bash
systemctl stop rabbitmq-server

# Edit file:

nano /var/lib/rabbitmq/.erlang.cookie

# Paste nội dung giống hệt controller 1 (Nhớ phải chuẩn không thừa khoảng trắng cuối dòng)

# Rồi sau đó set quyền cho file trên 3 controller
chown rabbitmq:rabbitmq /var/lib/rabbitmq/.erlang.cookie
chmod 400 /var/lib/rabbitmq/.erlang.cookie
```

Bước tiếp đó ta sẽ **Join Cluster**

- Trên Controller 2 + 3:

```bash
systemctl start rabbitmq-server
rabbitmqctl stop_app
rabbitmqctl reset
rabbitmqctl join_cluster rabbit@controller1
rabbitmqctl start_app
```

Sau đó ta sẽ kiểm tra Cluster

- Trên bất kì controller nào:

```bash
rabbitmqctl cluster_status

# Phải thấy

{running_nodes,[rabbit@controller1,
                rabbit@controller2,
                rabbit@controller3]}
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_41.png)

Tiếp theo ta tạo user cho OpenStack:(Trên Node Controller 1)

```bash
rabbitmqctl add_user openstack Tien@123
rabbitmqctl set_permissions openstack ".*" ".*" ".*"
# Form set_permissions <user> <conf> <write> <read>
# ".*" là trao tất cả quyền
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_42.png)

### `Bước 6`: Cài memcached

Trên 3 Node Controller cùng config như sau :

```bash
# Cài memcached

apt install memcached python3-memcache -y

# Sửa file
nano /etc/memcached.conf

# Tìm dòng
-l 127.0.0.1

# Sửa thành
-l 0.0.0.0

# Restart
systemctl restart memcached
systemctl enable memcached
```

=> Sau bước này ta lại làm mới volume snapshot để chuyển qua bước tiếp

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_43.png)

### `Bước 7`: Cài + Config Keepalived + HA Proxy

`Lưu ý`: Nhớ cái cấu hình `safe_to_bootstrap=1` sau mỗi lần snapshot

Trên 3 Node Controller ta cấu hình kiến trúc HaProxy với đường VIP để đáp ứng điều kiện sau:

- Cân bằng tải API giữa 3 controller
- Frontend 1 VIP (VD: 10.10.10.100)
- Backend: controller1, controller2, controller3
- Tất cả Service OpenStack sẽ cấu hình vào VIP này thay vì từng Controller
- `Keepalived` **tạo và quản lí** `VIP`:

  - 1 Node Master
  - 2 Node Backup
- `HAProxy` làm LoadBalance cho các services (MariaDB, Keystone, RabbitMQ…)

Đầu tiên, ta cài `keepalived`+ `HAProxy`:

- Trên cả 3 node Controller:

```bash
apt install keepalived -y
apt install haproxy -y
```

- Enable `keepalived`+ `HAProxy`:

```bash
systemctl enable keepalived
systemctl enable haproxy
# ! Không được start service
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_44.png)

Tiếp theo cấu hình `HAProxy`.Trên 3 Node:

```bash
#Lệnh này nói với Linux: "Cứ cho HAProxy chạy đi, IP VIP chưa có trên máy này nhưng lát nữa sẽ có
echo "net.ipv4.ip_nonlocal_bind = 1" >> /etc/sysctl.conf
sysctl -p

# Sửa file
nano /etc/haproxy/haproxy.cfg

# Xoá config cũ và thêm phần này vào cuối file:
global
    log /dev/log local0
    maxconn 4096
    daemon
    stats socket /run/haproxy/admin.sock mode 660 level admin

defaults
    retries 3
    option redispatch
    log global
    mode tcp
    option tcplog
    timeout connect 10s
    timeout client  1m
    timeout server  1m

# MariaDB - Lắng nghe trên VIP External
frontend galera_front
    bind 192.168.70.100:3306
    default_backend galera_back

backend galera_back                
    balance leastconn
    option tcp-check
    # Chuyển tiếp về IP nội bộ của 3 node
    server controller1 10.10.10.11:3306 check
    server controller2 10.10.10.12:3306 check backup 
    server controller3 10.10.10.13:3306 check backup

# RabbitMQ - Lắng nghe trên VIP External
frontend rabbit_front
    bind 192.168.70.100:5672
    default_backend rabbit_back

backend rabbit_back
    balance leastconn
    option tcp-check
    # Chuyển tiếp về IP nội bộ của 3 node
    server controller1 10.10.10.11:5672 check inter 3s rise 2 fall 3
    server controller2 10.10.10.12:5672 check inter 3s rise 2 fall 3
    server controller3 10.10.10.13:5672 check inter 3s rise 2 fall 3

# Về sau khi cài Keystone, Nova, Horizon, bạn cũng viết tiếp block frontend/backend tương tự, ví dụ: Horizon thì bind 192.168.70.100:80, trỏ về 10.10.10.x:80

# Qua Config VIP rồi mới restart thì HAProxy nó nhận VIP
```

Rồi sau đó, ta cấu hình `keepalived`

- Cấu hình Controller 1 (`MASTER NODE`):

```bash
nano /etc/keepalived/keepalived.conf

# Cấu hình
global_defs {
    script_user root
    enable_script_security
}

vrrp_script chk_haproxy {
    script "/usr/bin/pgrep haproxy"  # Keepalived phiên bản mới rất khắt khe về việc chạy script kiểm tra (chk_haproxy)
    interval 2
    weight -40              # Nếu HaProxy Master lỗi sẽ trừ priority đi 40 và trao VIP cho node Backup
}

vrrp_instance VN_1 {        # Tạo 1 instance vrrp có tên VN_1
    state MASTER            # Node Master để giữ VIP
    interface eth2          # VIP gắn vào interface này
    virtual_router_id 51    # ID cụm vrrp
    priority 150            # Độ ưu tiên (cao nhất là node master)
    advert_int 1            # Time gửi gói vrrp

    authentication {        # Cơ chế bảo mật giữa các node
        auth_type PASS      # Kiểu xác thực Pass
        auth_pass Tien@123  # Mật khẩu dùng giữa các node
    }
    virtual_ipaddress {     # Đây là VIP
        192.168.70.100/24   # Keepalived tự add/remove khi failover
    }

    track_script {
    chk_haproxy
    } 
}

# Check cấu hình haproxy đúng chưa
haproxy -c -f /etc/haproxy/haproxy.cfg

# nếu hiện valid là OKE
```

- Cấu hình Controller 2,3 (`BACK NODE`)

```bash
# Đối với Controller 2 đổi 2 dòng sau và giữ nguyên all
state BACKUP
priority 120
nopreempt      # Thêm dưới cấu hình vrrp_instance

# Đối với Controller 3 đổi 2 dòng sau và giữ nguyên all
state BACKUP
priority 100
nopreempt
```

Sau đó Restart với thứ tự đúng(Làm trên NODE MASTER trước rồi đến node piority thấp hơn !):

- Restart `keepalived`:

```bash
systemctl restart keepalived

# Check (3-5s)
ip a

# Phải thấy VIP xuất hiện trên eth2 rồi mới restart HAProxy
```

- Sau khi VIP tồn tại restart `HAProxy`:

```bash
systemctl restart haproxy
```

=> Làm trên 2 NODE BACKUP là được

Sau khi cấu hình hết xong sẽ check qua lại tất cả rồi đến bước cuối thực hiện `failover`:

```bash
# Check keepalived làm tốt việc giữ VIP không:(Trên Node Master)
ip a show eth2
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_46.png)

```bash
# Check HA Proxy lắng nghe đúng cổng không(Phải thấy dòng haproxy)
netstat -tulpn | grep -E '3306|5672'
```

Trong đó:

- **Dòng 1 & 4**: Bạn thấy `192.168.70.100:5672` và `192.168.70.100`:3306 đang được xử lý bởi `haproxy`. Đây là "điểm chạm" duy nhất mà các dịch vụ OpenStack sau này (`Keystone`, `Nova`...) sẽ gọi tới.

- **Dòng 3 & 5**: Bạn thấy `10.10.10.11:5672` (RabbitMQ) và `10.10.10.11:3306` (MariaDB) đang lắng nghe trên IP nội bộ. Điều này đảm bảo HAProxy có thể đẩy traffic vào đúng các dịch vụ chạy thực tế trên từng node.

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_47.png)

```bash
# Test thử kết nối DB qua VIP (Quan trọng nhất):
mysql -u root -p -h 192.168.70.100 -e "SHOW STATUS LIKE 'wsrep_cluster_size';"

#Nhập Mk:Tien@123
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_48.png)

### `Bước 7.5`: DEBUG 3 NODE ĐỀU NHẬN VIP

Căn bản các bước cấu hình `HAProxy` và `Keepalived` đều đúng nhưng vẫn gặp tình trạng `split_brain` đó là cả 3 Node đều nhận được VIP

Có thể giải thích đơn giản như sau:

- `Keepalived` các node nói chuyện với nhau qua gói tin `Multicast` để bầu chọn Master. Nếu các node không thấy nhau (do Firewall chặn hoặc hạ tầng OpenStack bên dưới chặn `Multicast`), cả 3 node sẽ đều tưởng mình là Master và tự gán VIP. Mà mình đã tắt firewall rồi lên chắc chắn do hạ tầng dải `192.168.70.x` drop bỏ gói tin.

Giờ ta DEBUG lại theo các bước sau:

#### `Bước 7.5.1`: Chuyển gói tin VIP gửi theo dạng UniCast (Viewer bỏ bước này xuống bước `7.5.3` vì 2 bước đầu sai luôn)

Sửa file `/etc/keepalived/keepalived.conf` trên CẢ 3 NODE như sau:

- Tại Controller 1 (Master)

```bash
vrrp_instance VN_1 {
    state MASTER
    interface eth2
    virtual_router_id 51
    priority 150
    # ... các cấu hình cũ giữ nguyên ...

    unicast_src_ip 192.168.70.72   # IP thực của eth2 trên C1
    unicast_peer {
        192.168.70.74              # IP thực của C2
        192.168.70.76              # IP thực của C3
    }
    vrrp_garp_master_delay 5
}
```

- Tại Controller 2 & 3 (Backup):

```bash
# Trên Controller 2
vrrp_instance VN_1 {
    state BACKUP
    ...
    unicast_src_ip 192.168.70.74   # IP thực của C2
    unicast_peer {
        192.168.70.72              # IP thực của C1
        192.168.70.76              # IP thực của C3
    }
    vrrp_garp_master_delay 5
}

# Cứ như vậy viết Controller3 tương tự với src IP 192.168.70.76
```

- `Stop Keepalived` trên cả 3 node.
- `Start Keepalived` trên `Controller 1` trước. (Đợi 5 giây check ip a xem có `VIP` chưa).
- `Start Keepalived` trên Controller 2 và 3.

Cuối cùng check thử xem đến bước này 3 Controller còn nhận VIP không:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_51.png)

=> Vẫn nhận VIP . Vì vậy ta chuyển sang giải pháp 2:

#### `Bước 7.5.2`: Sử dụng "Allowed Address Pairs"

Cấu hình này "khai báo" thêm với hạ tầng rằng: "Cái Port này ngoài IP chính (.72), tôi cho phép nó dùng thêm IP phụ là .100 (VIP) nữa".

Cách làm:

- Stop `haproxy` & `keepalived`
- Trong giao diện OpenStack hạ tầng (`Dashboard`), bạn vào `Network` -> `Ports`.
- Tìm Port của `controller1` (dải `192.168.70.x`).
- Trong phần cấu hình Port đó, tìm mục `Allowed Address Pairs`.
- Thêm IP: `192.168.70.100` (MAC có thể để trống hoặc lấy đúng MAC của Port đó).
- Bật lại `haproxy` & `keepalived`
- Làm tương tự cho **controller 2 và 3**.

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_52.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_53.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_54.png)

**Kết quả**:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_55.png)

=> OpenStack hạ tầng vừa chặn gói tin MultiCast vừa chặn địa chỉ MAC lạ và cả không cho phép tạo "Allowed Address Pair". Cho nên ta sẽ chuyển qua giải pháp 3

#### `Bước 7.5.3`: Chuyển Keepalived sang giải hạ tầng nội bộ 10.10.10.x

Stop `haproxy` & `keepalived`
Cấu hình lại file `keepalived`:

- Trên Node 1:

```bash
nano /etc/keepalived/keepalived.conf

# Cấu hình
global_defs {
    script_user root
    enable_script_security
}

vrrp_script chk_haproxy {
    script "/usr/bin/pgrep haproxy"  # Keepalived phiên bản mới rất khắt khe về việc chạy script kiểm tra (chk_haproxy)
    interval 2
    weight -40
}

vrrp_instance VN_1 {        # Tạo 1 instance vrrp có tên VN_1
    garp_master_delay 5
    garp_master_repeat 3
    garp_master_refresh 10
    vrrp_garp_master_repeat 5
    state MASTER            # Node Master để giữ VIP
    interface eth3          # VIP gắn vào interface này
    virtual_router_id 51    # ID cụm vrrp
    priority 150            # Độ ưu tiên (cao nhất là node master)
    advert_int 1            # Time gửi gói vrrp

    authentication {        # Cơ chế bảo mật giữa các node
        auth_type PASS      # Kiểu xác thực Pass
        auth_pass Tien@123  # Mật khẩu dùng giữa các node
    }
    virtual_ipaddress {     # Đây là VIP
        10.10.10.100/24     # Keepalived tự add/remove khi failover
    }

    track_script {
    chk_haproxy
    }
    unicast_src_ip 10.10.10.11  # IP thực của eth2 trên C1
    unicast_peer {
        10.10.10.12             # IP thực của C2
        10.10.10.13             # IP thực của C3
    }
}
```

- Trên Node 2 & 3:

```bash
# Đối với Controller 2 đổi các dòng sau và giữ nguyên all
state BACKUP
priority 120
nopreempt      # Thêm dưới cấu hình vrrp_instance
# Sửa unicast_src_ip là IP của chính nó và unicast_peer trỏ về 2 node còn lại.

# Đối với Controller 3 đổi các dòng sau và giữ nguyên all
state BACKUP
priority 120
nopreempt      # Thêm dưới cấu hình vrrp_instance
# Sửa unicast_src_ip là IP của chính nó và unicast_peer trỏ về 2 node còn lại.
```

Cấu hình lại HAProxy:(Trên 3 Node)

```bash
nano /etc/haproxy/haproxy.cfg

# Cấu hình
global
    log /dev/log local0
    maxconn 4096
    daemon
    stats socket /run/haproxy/admin.sock mode 660 level admin

defaults
    retries 3
    option redispatch
    log global
    mode tcp
    option tcplog
    timeout connect 10s
    timeout client  1m
    timeout server  1m

# MariaDB Galera
frontend galera_front
    bind 10.10.10.100:3306
    default_backend galera_back

backend galera_back
    balance leastconn
    option tcp-check
    server controller1 10.10.10.11:3306 check
    server controller2 10.10.10.12:3306 check backup
    server controller3 10.10.10.13:3306 check backup

# RabbitMQ
frontend rabbit_front
    bind 10.10.10.100:5672
    default_backend rabbit_back

backend rabbit_back
    balance leastconn
    option tcp-check
    server controller1 10.10.10.11:5672 check inter 3s rise 2 fall 3
    server controller2 10.10.10.12:5672 check inter 3s rise 2 fall 3
    server controller3 10.10.10.13:5672 check inter 3s rise 2 fall 3

# Horizon Dashboard
frontend horizon_front
    bind *:80
    mode http
    default_backend horizon_back

backend horizon_back
    balance source
    mode http
    option httpchk GET /auth/login
    server controller1 10.10.10.11:80 check inter 2s
    server controller2 10.10.10.12:80 check inter 2s
    server controller3 10.10.10.13:80 check inter 2s
```

Khởi động và Kiểm tra `VIP`

- Start Controller1: `systemctl start keepalived` -> `ip a show eth3` (Có `VIP`) -> `start HAProxy` (Cầm `VIP` trên node này)

- Start C2 & C3: `systemctl start keepalived` -> `start HAProxy` -> `ip a show eth3` (Không cầm `VIP`)

Kết quả:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_56.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_57.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_58.png)

=> Node 1 (`MASTER`) đã nhận `VIP` 2 Node kia chưa nhận là `OKE`

Xem lại quy hoạch port: (Trên 1 Node bất kì)

```bash
netstat -tulpn | grep -E '3306|5672'
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_59.png)

Trong đó:

- `10.10.10.11:3306 (mariadbd)` đang lắng nghe trên IP thực.
- `10.10.10.11:5672 (beam.smp - RabbitMQ)` đang lắng nghe trên IP thực.

=> Điều này giúp các node trong cluster tự đồng bộ dữ liệu với nhau qua IP định danh riêng.

- `10.10.10.100:3306 (haproxy)` lắng nghe trên `VIP`.
- `10.10.10.100:5672 (haproxy)` lắng nghe trên `VIP`.

=> Tất cả các request từ OpenStack Services (`Keystone`, `Nova`...) sẽ đi vào đây. `HAProxy` sẽ đứng ra **điều phối traffic**.

`LƯU Ý QUAN TRỌNG CHO CÁC PHASES TIẾP`: Bây giờ, khi bạn chuyển sang `PHASE 2` (Keystone) và các Phase sau, hãy luôn ghi nhớ nguyên tắc cấu hình địa chỉ:

- **Connection Strings (Database)**: Trong file `.conf`, khi khai báo link DB, bạn phải trỏ về `VIP` nội bộ:  

`connection = mysql+pymysql://keystone:Tien@123@10.10.10.100/keystone`

- **Transport URL (RabbitMQ)**: Tương tự với RabbitMQ:  
`transport_url = rabbit://openstack:Tien@123@10.10.10.100:5672/`

- **Endpoints**: Khi tạo endpoint cho OpenStack bằng lệnh openstack endpoint create, bạn cũng phải dùng URL trỏ về VIP:
`http://10.10.10.100:5000/v3`

### `Bước 8`: Test FailOver (Có kiểm soát)

Ta sẽ chia bước này sẽ chia ra làm các bước nhỏ khác:

#### `Bước 8.1`: Chuẩn bị các công cụ giám sát (Monitoring)

Mở 3 tab terminal (hoặc dùng tmux/screen) để theo dõi biến động thời gian thực:

- `Tab 1` (Theo dõi VIP trên Master - Controller 1):

```bash
watch -n 1 "ip a show eth2 | grep 10.10.10.100"
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_49.png)

- `Tab 2` (Theo dõi Log Keepalived trên Backup - Controller 2):

```bash
journalctl -u keepalived -f 
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_50.png)

- Trong đó:

  - **Dòng 3 & 4**: `Keepalived` khởi động xong và tạm thời đứng ở chế độ chờ (Backup).
  - **Dòng 5& 6 & 7**: Lúc này Keepalived chạy script `pgrep haproxy` nhưng không tìm thấy tiến trình HAProxy đang chạy (`status=1`).(Nó sẽ trừ piority `weight-40`như script)
  - **Dòng 8& 9& 10**: file Script thấy tiền trình HAProxy trên `MASTER` vẫn chạy (`status=0`) chứng tỏ Node MASTER vẫn ổn lên nó sẽ cộng lại priority và trả lại trạng thái ban đầu là NODE `BACKUP`

- `Tab 3` (Liên tục gọi vào DB qua VIP):  

```Bash
# Lệnh này sẽ cứ 1 giây kiểm tra xem DB còn sống không thông qua VIP:
while true; do mysql -u root -p'Tien@123' -h 10.10.10.100 -e "select @@hostname;" 2>/dev/null; sleep 1; done

# Ta sẽ thấy nó in ra liên tục tên host đang xử lý
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_60.png)

#### `Bước 8.2`: Tiến hành "Down" Node Master

Bây giờ, hãy giả lập tình huống **Controller 1** bị sập nguồn hoặc mất mạng. Trên **Controller 1**, hãy tắt card mạng chứa VIP hoặc tắt luôn service Keepalived:

- **Cách nhẹ nhàng**: `systemctl stop keepalived`(Maria DB vẫn sống)

- **Cách quyết liệt (giống thật hơn)**: `ifconfig eth2 down` (hoặc `ip link set eth2 down`)

#### `Bước 8.3`: Quan sát hiện tượng

Tại `Tab 1`: Dòng IP VIP `10.10.10.100` biến mất ngay lập tức.

Tại `Tab 2`: Bạn sẽ thấy log nhảy dòng: Entering MASTER STATE và advancing to MASTER state. Lúc này lệnh ip a show eth2 trên C2 sẽ thấy VIP xuất hiện.

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_61.png)

Tại `Tab 3`: Có thể bị khựng lại khoảng 1-2 giây (thời gian bầu chọn Master mới).

- Khi chưa tắt hẳn Node 1 MariaDB vẫn còn sống, `@@hostname` sẽ ra:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_62.png)

- Khi tắt hẳn Node 1 thì nó mới chuyển sang hiện controller 2:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_63.png)

#### `Bước 8.4`: Test Phục hồi (Recovery)

Sau khi Controller2 đã cầm VIP, hãy bật lại Controller1:

```bash
ifconfig eth2 up
systemctl start keepalived
```

Lúc này sẽ có hoạt động xảy ra:

- **Controller1 để Priority cao hơn**: Bạn sẽ thấy C2 nhả `VIP` (Entering BACKUP STATE) và C1 lấy lại `VIP` (Entering MASTER STATE). Đây gọi là cơ chế `Preempt`.

-> Kiểm tra lại `Tab 3`, mọi thứ vẫn phải thông suốt.

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_64.png)

#### `Bước 8.5`: Test HAProxy (Chết dịch vụ nhưng node vẫn sống)

Thử stop MariaDB trên node đang giữ VIP:

```bash
systemctl stop mariadb
```

=> Lúc này `VIP` vẫn ở node đó (vì `Keepalived` vẫn sống), nhưng HAProxy phải đủ 'thông minh' để biết `MariaDB` ở node đó chết rồi và đẩy request sang MariaDB của 2 node còn lại.

Check `Tab 3`: Nếu kết quả `@@hostname` chuyển sang node 2 và không báo lỗi `Can't connect to MySQL là HAProxy` check health chuẩn.

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_65.png)

Xong khi xong xuôi các trường hợp FailOver ta sẽ sang bước cài các Service tiếp theo.

### `Bước 9 <PHASE2>`: Cài đặt và cấu hình KeyStoneHA

Ta sẽ chia thành các bước nhỏ:

#### `Bước 9.1`: Tạo Database cho KeyStone (Làm trên Controller 1)

Đăng nhập vào `MariaDB` thông qua IP `VIP` để tận dụng cluster `Galera`:

```bash
mysql -u root -p -h 10.10.10.100
```

Tạo 1 DB dành cho service KeyStone và cấp quyền:

```bash
CREATE DATABASE keystone;
GRANT ALL PRIVILEGES ON keystone.* TO 'keystone'@'localhost' IDENTIFIED BY 'Tien@123';
GRANT ALL PRIVILEGES ON keystone.* TO 'keystone'@'%' IDENTIFIED BY 'Tien@123';
FLUSH PRIVILEGES;
EXIT;
```

Check:(Trên Node 2 & 3)

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_67.png)

#### `Bước 9.2`: Cài đặt các gói Keystone (Làm trên CẢ 3 Controller)

```bash
apt update
apt install keystone apache2 libapache2-mod-wsgi-py3 -y
```

**Check(3Node)**:

```bash
systemctl status apache2
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_70.png)

**Giải thích**: Đang gặp lỗi `ERROR` xung đột ở port `:80` do trong bài lab ta đã quy hoạch con `HAProxy` lẵng nghe port `:80` cho nên ta cấu hình lại **service Apache** lắng nghe trên port `:5000`(Cấu hình **service Apache** chính là đang cấu hình cho **service Keystone**)

**Debug**(3Node):

```bash
nano /etc/apache2/ports.conf
# Sửa Listen Port :80 thành Listen Port :5000
# Nếu không ta có thể xoá cả dòng Listen Port đi cũng được vì trong file vhost Keystone.conf khi ta tải Keystone về thì nó đã cấu hình port rồi(Ta làm theo cách này)
```

**Lưu ý**: Check `ls /etc/apache2/sites-enabled/` xem có bật cấu hình `default` không. Nếu **có** thì **tắt** đi thôi (Vì `default.conf` xài port `:80` )

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_71.png)

```bash
a2dissite 000-default
```

**Sau khi đã dọn dẹp port `:80`**:

```Bash
systemctl stop apache2
pkill -9 apache2  # Giết sạch các tiến trình treo nếu có
systemctl start apache2
systemctl status apache2
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_72.png)

**Một lưu ý nhỏ nữa**: Dòng `Could not reliably determine the server's fully qualified domain name` thực ra không làm chết service, nhưng nó sẽ hiện mỗi khi bạn restart. Để dẹp nó cho "sạch" log, bạn chạy lệnh này trên cả 3 node:

```Bash
echo "ServerName 10.10.10.12" >> /etc/apache2/apache2.conf
# Lưu ý: Ở node 1 thì thay bằng .11, node 3 thay bằng .13 cho chuẩn
systemctl reload apache2
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_73.png)

#### `Bước 9.3`: Cấu hình file Keystone (Làm trên CẢ 3 Controller)

Mở file `/etc/keystone/keystone.conf`:

```bash
nano /etc/keystone/keystone.conf

# Tìm và sửa các dòng sau (Bỏ dấu #):

[database]
# Trỏ connection về IP VIP
connection = mysql+pymysql://keystone:Tien@123@10.10.10.100/keystone

[token]
# Bắt buộc dùng fernet cho mô hình HA
provider = fernet
```

#### `Bước 9.4`: Sync Databases - Khởi tạo dữ liệu Databases (Chỉ làm Trên Node Controller 1)

```bash
su -s /bin/sh -c "keystone-manage db_sync" keystone
```

`Lưu ý`: Chỉ cần chạy trên Controller 1, Galera sẽ tự đồng bộ dữ liệu sang Node 2 và 3.

Check:

```bash
cat /var/log/keystone/keystone-manage.log
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_68.png)

=> Như vậy, Ta có thể thấy ta gặp rất nhiều lỗi ở đây nhưng chủ yếu bị lỗi `DBConnectionError`

#### `Bước 9.4`: Debug lỗi `DBConnectionError`

**Nguyên nhân gây lỗi**:

Trong chuỗi kết nối Database: `mysql+pymysql://keystone:Tien@123@10.10.10.100/keystone`

Hệ thống sẽ đi tìm ký tự `@` để phân tách giữa **Cụm User:Pass** và **Cụm Host (IP)**.

Vì mật khẩu của bạn là `Tien@123` (có dấu @), nên Python bị "lú". Nó tưởng user là `keystone`, pass là `Tien`, còn **Host/Server** là cái cụm còn lại: `123@10.10.10.100`.

Tất nhiên là không có cái máy chủ nào tên là `123@10.10.10.100` cả, nên nó báo lỗi `Name or service not known`.

**Cách Fix**:

`Cách 1`: **Mã hóa ký tự `@` (Khuyên dùng)**

Bạn không cần đổi pass, chỉ cần viết lại cái dấu `@` trong mật khẩu thành định dạng URL Encoding là `%%%%40`.

Mở file `/etc/keystone/keystone.conf`, sửa dòng connection thành:

```Ini, TOML
connection = mysql+pymysql://keystone:Tien%%%%40123@10.10.10.100/keystone
```

(Giải thích: `%%%%40` chính là mã hóa của dấu `@`. Lúc này Python sẽ hiểu mật khẩu là `Tien@123` và IP Server là `10.10.10.100`).

`Cách 2`: **Đổi mật khẩu dễ hơn**

Nếu bạn thấy cách 1 rắc rối, hãy đổi pass của user `keystone` trong MariaDB thành một chuỗi không có ký tự lạ (ví dụ: `Tien123`), sau đó sửa file config tương ứng:

```Ini, TOML
connection = mysql+pymysql://keystone:Tien123@10.10.10.100/keystone
```

=> **Ta sẽ chọn `Cách 2`**

**Debug**:

Xoá user, host và database đã tồn tại trên MySQL:

```bash
DROP USER 'keystone'@'%';
DROP USER 'keystone'@'localhost';
DROP DATABASES keystone;
```

Trên Node 1: Đổi sang mật khẩu `Tien123` trong MariaDB

```bash
CREATE DATABASE keystone;
GRANT ALL PRIVILEGES ON keystone.* TO 'keystone'@'localhost' IDENTIFIED BY 'Tien123';
GRANT ALL PRIVILEGES ON keystone.* TO 'keystone'@'%' IDENTIFIED BY 'Tien123';
FLUSH PRIVILEGES;
EXIT;
```

Cập nhật file `keystone.conf` trên 3 node -> Đổi sang mật khẩu mới không có kí tự -> Lưu cấu hình

Reload Service KeyStone:

```bash
systemctl reload apache2
```

**Sync** Databases(Trên Node 1):

```bash
su -s /bin/sh -c "keystone-manage db_sync" keystone
```

=> Sau đó node 2,3 tự cập nhật

Check:

```bash
cat /var/log/keystone/keystone-manage.log
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_69.png)

=> Đã `OKE`

#### `Bước 9.5`:Kiểm tra các bảng đã tạo trong MariaDB:

Trên một trong 3 Node bất kì .Check:

```bash
mysql -u keystone -pTien123 -h 10.10.10.100 -e "use keystone; show tables;"
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_75.png)

=> KeyStone của OpenStack tạo rất nhiều bảng

#### `Bước 9.5`: Thiết lập và khởi tạo Fernet Keys

Trên `NodeController1`:

```bash
keystone-manage fernet_setup --keystone-user keystone --keystone-group keystone
keystone-manage credential_setup --keystone-user keystone --keystone-group keystone
```

`LƯU Ý QUAN TRỌNG`: Bây giờ bạn phải copy toàn bộ thư mục `/etc/keystone/fernet-keys/` và `/etc/keystone/credential-keys/` từ `Node 1` sang `Node 2` và `Node 3`. Nếu không, khi bạn truy cập qua `VIP`, nếu request rơi vào `Node 2/3`, nó sẽ không giải mã được token và báo lỗi `500` hoặc `401`. Và nhớ phân quyền trên khi copy sang Node 2,3 để cho có thể truy cập file để lấy **key**

Vẫn trên `Node1`:

```bash
# Copy từ Node 1 sang Node 2
scp -r /etc/keystone/fernet-keys root@controller2:/etc/keystone/
scp -r /etc/keystone/credential-keys root@controller2:/etc/keystone/
# Tương tự cho controller3
scp -r /etc/keystone/fernet-keys root@controller3:/etc/keystone/
scp -r /etc/keystone/credential-keys root@controller3:/etc/keystone/
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_76.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_77.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_78.png)

Ở đây:

- Hệ thống sẽ lưu "dấu vân tay" (fingerprint) của controller2 vào file ~/.ssh/known_hosts trên controller1.
- Sau đó, nó sẽ yêu cầu bạn nhập mật khẩu của user root trên controller2.
- Khi nhập xong, thư mục fernet-keys sẽ được chép sang.

Giải thích:

- Cả 3 node đều dùng chung một bộ chìa khoá Fernets Key
- Node 1 tạo ra Token thì Node 2 hoặc Node 3 cũng phải giải mã và xác thực được Token đó.
- Nếu key không đúng thì Node sẽ báo lỗi "Unauthorized"(`401`)

Phân quyền cho thư mục trên `Node 2,3` sau khi chuyển file sang:

```bash
chown -R keystone:keystone /etc/keystone/fernet-keys
chmod -R 700 /etc/keystone/fernet-keys

chown -R keystone:keystone /etc/keystone/credential-keys
chmod -R 700 /etc/keystone/credential-keys

# Kiểm tra lại kết quả
ls -ld /etc/keystone/fernet-keys
ls -ld /etc/keystone/credential-keys
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_79.png)

#### `Bước 9.6`: Bootstrap KeyStone

Trên `Controller 1`: Khai báo endpoint cho Keystone. Chú ý tất cả URL đều phải trỏ về IP VIP (10.10.10.100):

```bash
keystone-manage bootstrap --bootstrap-password Tien123 \
  --bootstrap-admin-url http://10.10.10.100:5000/v3/ \
  --bootstrap-internal-url http://10.10.10.100:5000/v3/ \
  --bootstrap-public-url http://10.10.10.100:5000/v3/ \
  --bootstrap-region-id RegionOne
```

Restart lại apache và xóa database `SQLite` mặc định(Trên 3Node - Check có hẵng xoá ):

```bash
rm -f /var/lib/keystone/keystone.db
```

#### `Bước 9.7`: Bổ sung Keystone vào HAProxy

Để máy khách có thể gọi Keystone qua VIP, bạn cần khai báo port 5000 vào `HAProxy`. Làm trên CẢ 3 Controller:

```bash
nano /etc/haproxy/haproxy.cfg

# Thêm cấu hình
# Keystone API
frontend keystone_front
    bind 10.10.10.100:5000
    default_backend keystone_back

backend keystone_back
    balance roundrobin
    option tcp-check
    server controller1 10.10.10.11:5000 check inter 2000
    server controller2 10.10.10.12:5000 check inter 2000
    server controller3 10.10.10.13:5000 check inter 2000

# Check syntax valid không:
haproxy -c -f /etc/haproxy/haproxy.cfg

# Cấu hình cho KeyStone trên từng Node chỉ lắng nghe IP thật của nó
nano /etc/apache2/sites-available/keystone.conf

# Ví dụ trên Node 1
Listen 5000
<VirtualHost *:5000>

# Sửa thành
Listen 10.10.10.11:5000
<VirtualHost 10.10.10.11:5000>

# => Tương tự Node 2,3 cũng làm như vậy nhg nhớ thay IP của Node đó

# Reload, Restart các Service là được(Cả 3 Node)
a2ensite keystone
a2dissite 000-default
systemctl reload apache2
systemctl restart apache2
systemctl restart haproxy
...
```

#### `Bước 9.8`: Test lại toàn bộ KeyStone

Trên `Controller 1`, bạn tạo một file tên là `admin-openrc` để chứa biến môi trường(Nó giống giấy thông hành cho `admin` có quyền quản lí ):

```bash
nano admin-openrc

# Dán
export OS_PROJECT_DOMAIN_NAME=Default   # Xác định Domain chứa Project
export OS_USER_DOMAIN_NAME=Default      # Xác định Domain chứa User 
export OS_PROJECT_NAME=admin            # Tên của Project:admin - project quyền cao nhất 
export OS_USERNAME=admin                # Tên user đăng nhập vào hệ thống OPS
export OS_PASSWORD=Tien123              # Mật khẩu của user admin
export OS_AUTH_URL=http://10.10.10.100:5000/v3 # endpoint (KeyStone API)
export OS_IDENTITY_API_VERSION=3
export OS_IMAGE_API_VERSION=2
```

Check:

```bash
# Tải gói openstack client(Dùng điều khiển OPS thui)
apt install python3-openstackclient -y

# Nạp biến môi trường
source admin-openrc

# Kiểm tra Token
openstack token issue
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_80.png)

### `Bước 10 <PHASE3>`: Cài đặt và cấu hình Glance HA kết hợp với Backend Ceph RDB

Trước khi làm bước này hãy **Snapshot** 3 con Controller. Sau đó, ta cũng chia thành các bước nhỏ:

#### Giải pháp lưu trữ Image (Glance)

Trong môi trường **High Availability** (HA) với 3 Controller, việc chọn giải pháp lưu trữ cho Glance là cực kỳ quan trọng. Nếu bạn để mặc định (mỗi node lưu vào ổ cứng riêng), khi HAProxy đẩy yêu cầu sang node khác, hệ thống sẽ báo lỗi vì không tìm thấy file ảnh máy ảo.

Dưới đây là 3 giải pháp phổ biến, đi từ dễ đến khó:

**Giải pháp 1: Sử dụng NFS (Dễ nhất cho Lab)**: Đây là cách nhanh nhất để bạn có thể chạy được cụm HA mà không tốn quá nhiều tài nguyên. Bạn cần một máy chủ (hoặc dùng chính Controller 1 nếu Lab nhỏ) làm `NFS Server` để chia sẻ một thư mục cho cả 3 node Controller cùng mount vào.

**Giải pháp 2: Sử dụng Ceph RBD (Chuẩn công nghiệp)**:Đây là giải pháp mà ảnh máy ảo sẽ được chia nhỏ và lưu trữ phân tán trên nhiều node. Nếu một node chết, dữ liệu vẫn an toàn và các node khác vẫn truy cập bình thường.

- `Ưu điểm`: Khả năng mở rộng cực lớn, hiệu năng cao, hỗ trợ tính năng Live Migration (chuyển máy ảo đang chạy mà không ngắt quãng).

- `Nhược điểm`: Cực kỳ phức tạp để cài đặt lần đầu (cần tối thiểu 3 node lưu trữ riêng biệt).

**Giải pháp 3: Sử dụng Swift (Object Storage của OpenStack)**: Bạn cài đặt thêm dịch vụ Swift trong cụm OpenStack để lưu trữ Image như một Object (giống như cách Amazon S3 hoạt động).

- `Ưu điểm`: Tích hợp sẵn trong hệ sinh thái OpenStack.

- `Nhược điểm`: Tốn tài nguyên vì phải cài thêm một bộ dịch vụ mới (Swift Proxy, Swift Storage).

=> Do sơ đồ ta đã sử dụng 3 Node Storage Node dùng `backend Ceph RDB` cho lên ta sẽ chọn **giải pháp 2** làm giải pháp lưu trữ image cho Glance service và Mỗi node Ceph ta sẽ triển khai 1 OSD

#### `Bước 10.1`: Triển khai cụm Ceph Cluster HA cho việc lưu trữ image (Glance)

Trên 3 `StorageNode`: Ta sẽ cấu hình các service: `ceph-mon`(CephHA), `ceph-mgr`, `ceph-osd` và tạo các pool:

- images  -> Dùng để lưu trữ service Glance
- volumes -> Dùng để lưu trữ service Cinder RBD volumes
- backups -> Dùng để lưu trữ service Cinder backups
- VMs

Đầu tiên, ta ping để check thử xem cả 3 `NodeStrorage` có thông với `NodeController` không:

```bash
ping 10.10.30.34
ping 10.10.30.35
ping 10.10.30.36
```

Ngoài ra, ta tắt PortSecurity trên 2 dải `10.10.10.x` và `10.10.10.30` trên 3 Node Strorage để cấu hình cho thuận tiện:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_86.png)

Tiếp theo, ta check xem hiện tại các Node Storage đang phân vùng ổ địa hiện tại như thế nào: (Check trên 3 Node Storage)

```bash
lsblk
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_85.png)

=> Như ta có thể thấy ta có ổ (vda) là ổ chính để boot OS và ta sẽ cần thêm 1 ô phụ nữa. Cho nên, ở OpenStack hạ tầng ta sẽ `Attach Volume` vào Instances Storage 1 ổ phụ làm nơi chứa riêng biệt cho **OSD**

`Attach Volume` phụ để cấu hình OSD trên 3 NodeStorage:

- **Vào mục Volumes**: Tạo một Volume mới (ví dụ đặt tên là `OSDDisk_StorageNode_1`, dung lượng 120GB hoặc tùy ý).

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_87.png)

- **Attach Volume**: Chọn cái Volume vừa tạo -> `Manage Attachments` -> Chọn `Instance storage1`.

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_88.png)

- **Lặp lại**: Làm tương tự cho storage2 và storage3.

Sau khi attach Volume xong ta check xem Volume phụ `vdb` trên 3 Node Stor có phân nhánh không:

```bash
lsblk
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_89.png)

Trên 3 `StorageNode`: Dù nó đang trống nhưng để chắc chắn không còn "rác" từ tool partition cũ, hãy chạy lệnh này (nhớ là `vdb` nhé, **Không** được nhầm sang `vda`):

```bash
sgdisk --zap-all /dev/vdb
wipefs -a /dev/vdb
```

**Chuẩn bị môi trường**:

```bash
apt update
apt install -y ceph-common ceph-mon ceph-mgr ceph-osd ceph-base radosgw

# Check version Ceph đang dùng
ceph -v
ceph version 19.2.3 (c92aebb279828e9c3c1f5d24613efca272649e62) squid (stable)
```

=> Bản `Ceph Squid (19.2.x)` cần cực kỳ cẩn thận với các phân quyền của `ceph-volume` và các thay đổi trong cấu trúc lớp bảo mật `cephx`. Và nhớ phân quyền file sau khi chuyển copy cấu hình từ `NodeStorage1` xuống `Node 2,3`

Mô hình Ceph của ta giờ có thể hiểu là:

```text
MON public IP:
  storage1: 10.10.30.31
  storage2: 10.10.30.32
  storage3: 10.10.30.33

Ceph cluster network:
  storage1: 10.10.40.41
  storage2: 10.10.40.42
  storage3: 10.10.40.43

Glance chạy trên controller:
  controller1: 10.10.30.34
  controller2: 10.10.30.35
  controller3: 10.10.30.36
```

=> Glance chỉ cần đi vào Ceph public/client network 10.10.30.0/24, không dùng dải 10.10.40.0/24

#### `Bước 10.2`: Bootstrap cho cụm Cephcluster HA

Trên `NodeStorage1`: **Khởi tạo ID** cho cụm cluster HA này (FSID - định danh cụm) và các file khoá xác thực cơ bản và tắt giao diện `DashBoard`:

```bash
UUID=$(uuidgen)

cat <<EOF > /etc/ceph/ceph.conf
[global]
fsid = $UUID                     
mon_initial_members = storage
mon_host = 10.10.30.31
public_network = 10.10.30.0/24
cluster_network = 10.10.40.0/24
auth_cluster_required = cephx
auth_service_required = cephx
auth_client_required = cephx
osd_pool_default_size = 3
osd_pool_default_min_size = 2

[mgr]
mgr_modules = 
EOF
```

#### `Bước 10.3`: Tạo `Keyrings` cho MON/admin/bootstrap-osd của Ceph cluster

```bash
# Tạo file lưu admin key ceph và file lưu osd key
mkdir -p /etc/ceph /var/lib/ceph/bootstrap-osd

# Tạo monitor secret key
ceph-authtool --create-keyring /tmp/ceph.mon.keyring --gen-key -n mon. --cap mon 'allow *'

# Tạo admin key
ceph-authtool --create-keyring /etc/ceph/ceph.client.admin.keyring \
  --gen-key -n client.admin \
  --cap mon 'allow *' \
  --cap osd 'allow *' \
  --cap mgr 'allow *' \
  --cap mds 'allow *'

# Tạo bootstrap-osd key
ceph-authtool --create-keyring /var/lib/ceph/bootstrap-osd/ceph.keyring \
  --gen-key -n client.bootstrap-osd \
  --cap mon 'profile bootstrap-osd' \
  --cap mgr 'allow r'

# Gộp hết vào mon keyring
ceph-authtool /tmp/ceph.mon.keyring --import-keyring /etc/ceph/ceph.client.admin.keyring
ceph-authtool /tmp/ceph.mon.keyring --import-keyring /var/lib/ceph/bootstrap-osd/ceph.keyring

# Phân quyền cho file chỉ có user ceph mới đc truy cập
chown ceph:ceph /tmp/ceph.mon.keyring
```

Tiếp theo, ta sẽ tạo monmap:

```bash
monmaptool --create \
  --add storage1 10.10.30.31 \
  --fsid "$UUID" \
  /tmp/monmap
```

=> Ban đầu chỉ add `storage1` thôi. `storage2/3` add sau khi MON đầu tiên chạy ổn.

Tiếp theo đó, Init MON đầu tiên:

```bash
mkdir -p /var/lib/ceph/mon/ceph-storage1

ceph-mon --mkfs -i storage1 \
  --monmap /tmp/monmap \
  --keyring /tmp/ceph.mon.keyring

chown -R ceph:ceph /var/lib/ceph/mon/ceph-storage1
systemctl enable --now ceph-mon@storage1
```

Check:

```bash
ceph -s
ceph mon stat
```

Rồi sau đó, tạo MGR trên Storage1:

```bash
mkdir -p /var/lib/ceph/mgr/ceph-storage1

ceph auth get-or-create mgr.storage1 \
  mon 'allow profile mgr' \
  osd 'allow *' \
  mds 'allow *' \
  -o /var/lib/ceph/mgr/ceph-storage1/keyring

chown -R ceph:ceph /var/lib/ceph/mgr/ceph-storage1
systemctl enable --now ceph-mgr@storage1
```

Check lại:

```bash
ceph -s
```

Output:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_90.png)

=> Tức là hiện tại cụm Ceph mới có 1 MON, chưa có MGR, chưa có OSD. Làm tiếp như sau.

=> `HEALTH_WARN` ta cấu hình thêm `msgr2`

Bật `msgr2` và fix warning cơ bản. Trên storage1:

```bash
ceph mon enable-msgr2
ceph config set mon auth_allow_insecure_global_id_reclaim false
```

Check lại:

```bash
ceph -s
```

Output:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_91.png)

=> Như này là `OKE`

=> Như vậy, bước Bootstrap cluster thành công giờ ta cấu hình thêm mgr rồi đồng bộ thứ tự các bước sang node Storage 2,3;mở rộng quorum lên 3 Node và rồi cuối cùng mới add 3 OSD `dev/vdb`.

#### `Bước 10.4`: Tạo MGR ,OSD trên StorageNode 1

Đầu tiên, MGR trên Storage 1:

```bash
mkdir -p /var/lib/ceph/mgr/ceph-storage1

ceph auth get-or-create mgr.storage1 \
  mon 'allow profile mgr' \
  osd 'allow *' \
  mds 'allow *' \
  -o /var/lib/ceph/mgr/ceph-storage1/keyring

chown -R ceph:ceph /var/lib/ceph/mgr/ceph-storage1

systemctl enable --now ceph-mgr@storage1
```

Check:

```bash
ceph -s
```

Output:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_92.png)

=> Như này là `OKE`

#### `Bước 10.5`: Cấu hình đồng bộ sang Storage Node 2,3

Đầu tiên, trên `Storage1` ta lấy monkey + monmap + các key rồi copy sang 2 Node còn lại:

```bash
# Lấy monkey + monmap
ceph auth get mon. -o /root/ceph.mon.keyring
ceph mon getmap -o /root/monmap

# Chuyển file ceph.conf + adminkey cho thằng còn lại
scp /etc/ceph/ceph.conf /etc/ceph/ceph.client.admin.keyring root@storage2:/etc/ceph/
scp /etc/ceph/ceph.conf /etc/ceph/ceph.client.admin.keyring root@storage3:/etc/ceph/

# Tạo phiên bootstrap cho 2 thằng còn lại
ssh root@storage2 'mkdir -p /var/lib/ceph/bootstrap-osd /etc/ceph'
ssh root@storage3 'mkdir -p /var/lib/cephx/bootstrap-osd /etc/ceph'

# Chuyển osdkey cho 2 thằng còn lại
scp /var/lib/ceph/bootstrap-osd/ceph.keyring root@storage2:/var/lib/ceph/bootstrap-osd/ceph.keyring
scp /var/lib/ceph/bootstrap-osd/ceph.keyring root@storage3:/var/lib/ceph/bootstrap-osd/ceph.keyring

# Chuyển monkey và monmap cho 2 thằng còn lại
scp /tmp/ceph.mon.keyring /tmp/monmap root@storage2:/tmp/
scp /tmp/ceph.mon.keyring /tmp/monmap root@storage3:/tmp/
```

Tiếp theo, Init MON trên 2 Storage Node còn lại:

```bash
# Trên Storage Node 2
mkdir -p /var/lib/ceph/mon/ceph-storage2

ceph-mon --mkfs -i storage2 \
  --monmap /tmp/monmap \
  --keyring /tmp/ceph.mon.keyring

chown -R ceph:ceph /var/lib/ceph/mon/ceph-storage2
systemctl enable --now ceph-mon@storage2

# Trên Storage Node 3
mkdir -p /var/lib/ceph/mon/ceph-storage3

ceph-mon --mkfs -i storage3 \
  --monmap /tmp/monmap \
  --keyring /tmp/ceph.mon.keyring

chown -R ceph:ceph /var/lib/ceph/mon/ceph-storage3
systemctl enable --now ceph-mon@storage3
```

Check Quorum trên Storage Node 1:

```bash
ceph -s
ceph mon stat
```

Output:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_93.png)

=> Nếu thấy quorum hiện cả 3 Node thì `OKE`

Nhưng sửa ceph config thì initinal member vẫn là mỗi thằng Storage Node 1 lên ta cần đồng bộ lại cả 3

```bash
# Trên Storage Node 1 ta sửa lại ceph.conf(Giữ nguyên UUID)
cat <<EOF > /etc/ceph/ceph.conf
[global]
fsid = 826a581a-b900-40c3-a897-c2636c2d942d                  
mon_initial_members = storage1,storage2,storage3
mon_host = 10.10.30.31,10.10.30.32,10.10.30.33
public_network = 10.10.30.0/24
cluster_network = 10.10.40.0/24
auth_cluster_required = cephx
auth_service_required = cephx
auth_client_required = cephx
osd_pool_default_size = 3
osd_pool_default_min_size = 2

[mgr]
mgr_modules = 
EOF

# Rồi sau đó copy chuyển cho 2 thằng còn lại:
scp /etc/ceph/ceph.conf root@storage2:/etc/ceph/ceph.conf
scp /etc/ceph/ceph.conf root@storage3:/etc/ceph/ceph.conf

# Restart MON từng Node một
systemctl restart ceph-mon@storage1
systemctl restart ceph-mon@storage2
systemctl restart ceph-mon@storage3
```

Cuối cùng, tạo MGR trên 2 Storage Node còn lại:

```bash
# Trên Storage 2:
mkdir -p /var/lib/ceph/mgr/ceph-storage2

ceph auth get-or-create mgr.storage2 \
  mon 'allow profile mgr' \
  osd 'allow *' \
  mds 'allow *' \
  -o /var/lib/ceph/mgr/ceph-storage2/keyring

chown -R ceph:ceph /var/lib/ceph/mgr/ceph-storage2
systemctl enable --now ceph-mgr@storage2

# Trên Storage 3:
mkdir -p /var/lib/ceph/mgr/ceph-storage3

ceph auth get-or-create mgr.storage3 \
  mon 'allow profile mgr' \
  osd 'allow *' \
  mds 'allow *' \
  -o /var/lib/ceph/mgr/ceph-storage3/keyring

chown -R ceph:ceph /var/lib/ceph/mgr/ceph-storage3
systemctl enable --now ceph-mgr@storage3
```

Lúc này về Storage 1 check Health toàn sluster:

```bash
ceph -s
ceph mon stat
ceph mgr stat
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_94.png)

=> OUTPUT hiện: `mgr: storage1(active), standbys: storage2, storage3`
là `OKE`

#### `Bước 10.6`: Add OSD

Đảm bảo disk `/dev/vdb` đang là `RAW` Disk (có thể check = `lsblk`)

Nếu chắc rồi chạy lệnh create data trên 3 node Storage:

```bash
ceph-volume lvm create --data /dev/vdb
```

**Note**: Nếu muốn cấu hình 2 OSD trên 1 Node thì ta có thể thêm `vdc` và cũng dùng lệnh tương tự như trên để tạo OSD thứ 2.(Và nhớ chỉnh cả file config cho chuẩn !)

Check lại trên Node Storage 1:

```bash
ceph -s
ceph osd tree
ceph osd stat
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_95.png)

=> OUTPUT `osd: 3 osds: 3 up, 3 in` là `OKE`

#### `Bước 10.7`: Tạo pool trên Ceph

Chạy trên `Storage 1` (Vì là 1 cluster đồng bộ rồi):

```bash
for p in images volumes vms backups; do
  ceph osd pool create $p 32
  ceph osd pool set $p size 3
  ceph osd pool set $p min_size 2
  ceph osd pool application enable $p rbd
  ceph osd pool set $p pg_autoscale_mode on
  rbd pool init $p
done

ceph -s
ceph osd pool ls
```

Output:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_96.png)

**Note**: Gán giá trị cho p lặp qua danh sách 4 giá trị: `images`, `volumes`, `vms`, `backups`. Mỗi lần lặp, biến `$p` nhận 1 giá trị trong danh sách đó:

- `ceph osd pool create $p 32` — tạo pool với 32 PG ban đầu. Con số 32 không quan trọng lắm vì bước tiếp theo bật autoscale, Ceph sẽ tự điều chỉnh.

- `size 3` — mỗi object có 3 bản sao (1 primary + 2 replica), cần tối thiểu 3 OSD.

- `min_size 2` — cluster vẫn cho phép I/O khi còn tối thiểu 2 bản sao, tránh cluster bị block hoàn toàn khi mất 1 OSD.

- `application enable $p rbd` — đánh dấu pool này dùng cho RBD, Ceph dùng thông tin này để tối ưu và tránh warning application not enabled.

- `pg_autoscale_mode on` — cho phép Ceph tự tính lại số PG phù hợp theo dung lượng thực tế của pool, không cần tính tay.

- `rbd pool init $p` — khởi tạo RBD metadata headers bên trong pool, bắt buộc trước khi Nova/Glance/Cinder dùng được.

#### `Bước 10.8`: Tạo user cho Glance

```bash
# Trên Storage Node 1
ceph auth get-or-create client.glance \
  mon 'profile rbd' \
  osd 'profile rbd pool=images' \
  mgr 'profile rbd pool=images' \
  -o /etc/ceph/ceph.client.glance.keyring

# Test:
rbd create images/test-glance --size 128 --id glance
rbd ls -p images --id glance
rbd rm images/test-glance --id glance
```

Output:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_97.png)

=> Ra `testglance` Như này là `OKE`(import images + xoá thử)

#### `Bước 10.9`: Cài gói Ceph client trên cả 3 Controller

Đầu tiên, cài gói ceph client trên cả 3 controller:

```bash
apt update -y
apt install -y ceph-common python3-rbd python3-rados
```

Rồi sau đó, copy file Ceph sang cả 3 Controller(Từ Storage Node 1):

```bash
# Từ Storage 1:
scp /etc/ceph/ceph.conf /etc/ceph/ceph.client.glance.keyring root@controller1:/etc/ceph/
scp /etc/ceph/ceph.conf /etc/ceph/ceph.client.glance.keyring root@controller2:/etc/ceph/
scp /etc/ceph/ceph.conf /etc/ceph/ceph.client.glance.keyring root@controller3:/etc/ceph/
```

Phân quyền cho service `glance` lấy được key trên 3 Node Controller:

```bash
chown root:root /etc/ceph
chmod 755 /etc/ceph

chown root:root /etc/ceph/ceph.conf
chmod 644 /etc/ceph/ceph.conf

chown glance:glance /etc/ceph/ceph.client.glance.keyring
chmod 640 /etc/ceph/ceph.client.glance.keyring
```

Test trên từng controller:

```bash
rbd ls -p images --id glance
```

=> Sau khi cài xong có thể dùng lệnh `rdb` trên Conttroller.

#### `Bước 10.10`: Tạo Database cho Glance

Chỉ cần thực hiện trên 1 Node Controller  (Galera tự đồng bộ sang Node 2,3)

```bash
mysql -u root -p -h 10.10.10.100

# Tạo database glance
CREATE DATABASE glance;

# Cấp quyền cho user glance 
GRANT ALL PRIVILEGES ON glance.* TO 'glance'@'localhost' IDENTIFIED BY 'Tien123';
GRANT ALL PRIVILEGES ON glance.* TO 'glance'@'%' IDENTIFIED BY 'Tien123';

FLUSH PRIVILEGES;
EXIT;
```

Check:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_81.png)

#### `Bước 10.11`: Khai báo với KeyStone (Chỉ làm trên 1 Node - Node 1)

```bash
# Chạy script biến môi trường: 
source admin-openrc

# Tạo user glance
openstack user create --domain default --password Tien123 glance

# Tạo domain project service cho mỗi dịch vụ glance, neutron, nova đều có tài khoản như 1 user
openstack project create --domain default --description "Service Project" service

# Add role admin cho user glance trong project service
openstack role add --project service --user glance admin

# Tạo service có tên glance
openstack service create --name glance --description "OpenStack Image" image

# Tạo các Endpoint trỏ về VIP (10.10.10.100)
openstack endpoint create --region RegionOne image public http://10.10.10.100:9292
openstack endpoint create --region RegionOne image internal http://10.10.10.100:9292
openstack endpoint create --region RegionOne image admin http://10.10.10.100:9292
```

Check:

```bash
openstack endpoint list
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_82.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_83.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_84.png)

#### `Bước 10.12`: Cài đặt và cấu hình thêm Glance API vào HA Proxy trên 3 Controller

```bash
nano /etc/haproxy/haproxy.cfg

# Dán cấu hình glance này vào dưới
frontend glance_api_front
    bind 10.10.10.100:9292
    default_backend glance_api_back

backend glance_api_back
    balance roundrobin
    option tcp-check
    server controller1 10.10.10.11:9292 check inter 2000
    server controller2 10.10.10.12:9292 check inter 2000
    server controller3 10.10.10.13:9292 check inter 2000

# Check file config OKE chưa:
haproxy -c -f /etc/haproxy/haproxy.cfg

# Reload backup trước, master sau:
# Check Node nào đang cầm VIP là Node Master
ip a | grep 10.10.10.100
systemctl reload haproxy
systemctl status haproxy --no-pager
```

#### `Bước 10.13`: Cài đặt và Cấu hình Glance (Làm trên cả 3 Node)

Đầu tiên, Ta sẽ cài đặt các gói cần thiết:

```bash
apt update
apt install glance -y
```

Tiếp theo đó, Ta sẽ cấu hình file `/etc/glance/glance-api.conf`

```bash
nano /etc/glance/glance-api.conf

# Đè cấu hình như sau:
# Tìm và thay thế dòng cũ bằng dòng dưới để cho phép chế độ ghi log với glance
# Nếu Controller 2,3 phải để bind_host=10.10.10.12,10.10.10.13

[DEFAULT]
bind_host = 10.10.10.11
bind_port = 9292
enabled_backends = ceph_backend:rbd
verbose = true

# Comment dòng (Thêm # đầu dòng)
#sqlite_db = /var/lib/glance/glance.sqlite

[database]
# Trỏ về VIP Database
connection = mysql+pymysql://glance:Tien123@10.10.10.100/glance

[keystone_authtoken]
# Thông tin xác thực với Keystone qua VIP
www_authenticate_uri = http://10.10.10.100:5000
auth_url = http://10.10.10.100:5000
memcached_servers = 10.10.10.11:11211,10.10.10.12:11211,10.10.10.13:11211
auth_type = password
project_domain_name = Default
user_domain_name = Default
project_name = service
username = glance
password = Tien123

[paste_deploy]
flavor = keystone

[glance_store]
# Nơi lưu trữ ảnh máy ảo
default_backend = ceph_backend

[ceph_backend]
rbd_store_pool = images
rbd_store_user = glance
rbd_store_ceph_conf = /etc/ceph/ceph.conf
rbd_store_chunk_size = 8
```

#### `Bước 10.14`: Đồng bộ Databases (Chỉ làm trên 1 Node Controller)

```bash
su -s /bin/sh -c "glance-manage db_sync" glance
```

Check DB, service, API qua VIP, Glance ghi thật vào Ceph:

```bash
# Check DB:
mysql -u glance -pTien123 -h 10.10.10.100 -e "USE glance; SHOW TABLES;"

# Check service
systemctl restart glance-api
systemctl status glance-api --no-pager

# Check Port
ss -lntp | grep 9292
source admin-openrc
openstack service show image
openstack endpoint list --service image

# API qua VIP
curl -s http://10.10.10.100:9292
openstack image list

# Glance ghi thật vào Ceph
dd if=/dev/zero of=/tmp/test-glance.raw bs=1M count=8

openstack image create test-glance-rbd \
  --file /tmp/test-glance.raw \
  --disk-format raw \
  --container-format bare \
  --public

openstack image show test-glance-rbd
rbd -p images ls --id glance
```
