# DEPLOY OPENSTACK HA 3 NODE CONTROLLER ON OPENSTACK

## I. ĐẶC ĐIỂM CỦA OPSTACK HA

### 1. Đặc điểm của HA trong OPS

**HA (High Availability)** = Tính sẵn sàng cao

Mục tiêu của HA với OPS:

#### **Đảm bảo không downtime (hoặc downtime tối thiểu)**

- Khi một node (controller, network, compute…) bị lỗi → hệ thống vẫn chạy

- Người dùng vẫn:

  - Tạo VM
  - Truy cập VM
  - Sử dụng dịch vụ bình thường

=> Ví dụ: `controller-1` chết → `controller-2/3` tự động tiếp quản

#### **Tự động failover (chuyển đổi khi có lỗi)**

- HA giúp:

  - Phát hiện node/service bị lỗi
  - Tự động chuyển sang node khác (failover)
  - Không cần can thiệp thủ công

=> Thường dùng:

- `Pacemaker` / `Corosync`
- `HAProxy` + `Keepalived` (VIP)

#### **Cân bằng tải (Load Balancing)**

- Nhiều node controller cùng xử lý request

- Tránh quá tải 1 node

=> Ví dụ:

- `API` (Nova, Keystone, Neutron) được load balance qua `HAProxy`

#### **Tăng độ tin cậy (Reliability)**

- Tránh single point of failure (SPOF)

Các thành phần cần HA:

- Controller nodes (`API`, scheduler…)
- Database (`MariaDB`; `Galera` cluster)
- Message Queue (`RabbitMQ` cluster)
- Network services (`L3 agent`, `DHCP`agent…)

#### Bảo vệ dữ liệu & trạng thái hệ thống

DB cluster đảm bảo:

- Không mất dữ liệu khi 1 node chết
- Dữ liệu được replicate giữa các node

#### Đảm bảo SLA (Service Level Agreement)

HA giúp hệ thống:

- Đạt uptime cao (99.9% → 99.99%)
- Phù hợp môi trường production

## II. MÔ HÌNH DEPLOY OPENSTACK HA

### 1. Mô hình cài đặt

Mô hình cài đặt OpenStack Flamingo trên môi trường OPS lab có sẵn:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_01.png)

### 2. IP planning

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_02.png)

### 3. Yêu cầu Từ OpenStack hạ tầng (Cụm bên ngoài )

Trước khi tạo VM cho cụm HA phải đảm bảo:

#### 3.1 Nested Virtualization

Trên compute node của OpenStack hạ tầng:

```bash
cat /sys/module/kvm_intel/parameters/nested

# Phải = `Y`
```

Nếu không → cụm HA bên trong sẽ không tạo được VM.

#### 3.2 Flavor cho VM của cụm HA

Tất cả VM controller/compute/storage phải:

- CPU mode: `host-passthrough`
- Expose `VMX`/`SVM`
- RAM đủ lớn
- Disk nên dùng volume (không dùng `ephemeral`)

#### 3.3 Network yêu cầu

- Cho phép `promiscuous mode`
- **Tắt port security** hoặc cấu hình `allowed-address-pairs`
- Hỗ trợ `Trunk` (nếu dùng VLAN)
- Kiểm tra `MTU` (khuyên dùng 1450 cho tenant network)

### 4. Yêu cầu hạ tầng chung trên các node & Service cần có trên từng node

#### 4.1 Cấu hình chung trên tất cả các node (Controller/Compute/Storage)

**OS**:

- Ubuntu 24.04 LTS (khuyến nghị)
- Cấu hình static IP
- Hostname chuẩn
- `/etc/hosts` đầy đủ toàn cluster

Ví dụ:

```bash
192.168.70.81  controller 1
192.162.70.83  controller 2
192.162.70.85  controller 3
```

**System config** bắt buộc:

- Disable swap
- Enable NTP (chrony)
- `net.ipv4.ip_forward = 1` (compute + network)
- `net.bridge.bridge-nf-call-iptables` = 1
- Tắt firewall hoặc mở port đầy đủ
- Cài gói cơ bản: `curl`, `wget`, `vim`, `net-tools`

**Network** theo layout:

| Network    | CIDR              | Dùng cho             |
| ---------- | ----------------- | -------------------- |
| MGMT + API | `10.10.10.0`/24   | Service nội bộ + VIP |
| Tenant     | `10.10.20.0`/24   | VXLAN                |
| Ceph       | `10.10.30.0`/24   | Storage backend      |
| Provider   | `192.168.70.0`/24 | Floating IP          |

#### 4.1 Controller Node

Mỗi controller sẽ chạy đầy đủ **Control Plane Service.**

Gồm:

- `HA Layer`

  - HA Proxy
  - Keepalived (VIP: `10.10.10.100`)

- `Database Layer`

  - MariaDB Galera Cluster (3 Node)
  - Port:

    - `3306`
    - `4567`
    - ``4568`
    - 4444`

- `Message Queue`

  - `RabbitMQ cluster` (3node)
  - Port:

    - `5672`
    - `25672`

- `Identity`

  - `Keystone`

- `Image`

  - `Glance` (backend Ceph RBD)

- `Compute Control`

  - `Nova API`
  - `Nova Scheduler`
  - `Nova Conductor`
  - `Placement API`

- Cache

  - `Memcached`

- `Networking Control`

  - `Neutron Server`
  - `ML2 plugin`
  - `DHCP agent`
  - `L3 agent` (nếu không dùng full DVR)

- `Block Storage control`

  - `Cinder API`
  - `Cinder Scheduler`
  - `Cinder Volume` (nếu dùng Ceph thì volume có thể ở storage node)

- `Dashboard`

  - Horizon

#### 4.2 Compute Node (x2 -DVR)

- Service cần có

  - `nova-compute`
  - `neutron-openvswitch-agent` (hoặc linuxbridge)
  - `neutron-l3-agent` (DVR mode)
  - `neutron-metadata-agent`
  - Virtualization

    - KVM
    - libvirt

Kiểm tra:

```bash
egrep -c '(vmx|svm)' /proc/cpuinfo
```

- Cần cấu hình Kernel (Kernel Config)

```bash
net.ipv4.ip_forward=1
net.bridge.bridge-nf-call-iptables=1
```

#### 4.3 Storage Node (x3 - Ceph)

**Chuẩn bị Disk**:

Mỗi OSD:

- 1 volume riêng (không dùng disk OS)
- Khuyến nghị raw volume

**Ceph service**:

- `ceph-mon` (3 node)
- `ceph-mgr`
- `ceph-osd`
- `rbd`

**Pool cần tạo**:

- images
- volumes
- backups
- `vms`

**Nếu chạy Cinder Volume tại đây**:

- `Cinder-volume`(RBD backend)

#### 4.4 Service Logic tổng thể

VIP (`10.10.10.100`) publish:

- `5000` -> `Keystone`
- `8874` -> `Nova`
- `9292` -> `Glance`
- `9696` -> `Neutron`
- `8776` -> `Cinder`
- `80` -> `Horizon`

### 5. Những khó khăn & lưu ý khi cài ManualInstall (so với các cách khác Automation or use Kolla-Ansible)

#### 5.1 HA manual config rất phức tạp

- Sai 1 dòng cấu hình `Galera` → **cluster split-brain**
- Sai cấu hình `Rabbit` → **message queue** không sync
- Sai endpoint → service không register

=> Manual = Rất dễ lệch config giữa các controller

Automation đảm bảo:

- File cấu hình giống nhau
- Restart theo đúng thứ tự
- Sync database migration đúng cách

=> Bản chất của Automation đã chạy các file bash scripts theo thứ tự

#### 5.2 Nested tăng độ khó

Ta đang chạy: OpenStack trên 1 OpenStack hạ tầng khác.

- Nested `KVM`
- Nested `VXLAN`
- Nested routing
- Nested security group

**Manual** cực dễ vỡ ở:

- `MTU` sai
- `iptables rule` xung đột
- `Bridge` mapping sai
- `DVR` không hoạt động do port security

Automation (Kolla/OpenStack-Ansible) xử lý:

- `Bridge` mapping tự động
- `ML2` config chuẩn
- `Sync neutron agent` đồng bộ

#### 5.3 HA yêu cầu cấu hình đồng nhất 100%

Ví dụ:

`nova.conf` trên 3 controller phải giống nhau 99%.

Chỉ cần khác:

```bash
transport_url
memcached_servers
connection
```

→ Service không sync.

=> `Manual` rất dễ sai.
=> `Automation` dùng template → đảm bảo đồng nhất.

#### 5.4 Database migration cực nguy hiểm khi manual

Trong HA:

Chỉ 1 node được chạy:

```bash
nova-manage db sync
```

Nếu **chạy nhầm nhiều node** → có thể **corrupt schema**.

`Automation` kiểm soát:

- Node nào chạy migration
- Chạy đúng thứ tự
- Lock đúng service

#### 5.5 Khi lỗi xảy ra, manual rất khó rollback

Ví dụ:

- `Galera` mất quorum
- `RabbitMQ` mất cluster
- `Keystone` token lỗi
- `Neutron` DVR không route

-> `Manual` sửa = mò từng node.

-> `Automation`: `Re-run playbook` là `self-heal`.

#### 5.6 NGUYÊN TẮC KHI CÀI MANUAL HA

- Chỉ chạy DB migration trên 1 controller duy nhất
- Mọi file config giữa các controller phải giống nhau 100%
- Tất cả endpoint phải trỏ về `VIP`
- `Ceph` phải chạy ổn định trước khi cài `Glance`/`Cinder`
- `Nested KVM` phải `OK` trước khi cài `nova-compute`.
- Test từng `PHASE` một khi được mới sang `PHASE` tiếp

### 6. Lưu ý

```text
- Tất cả VM node dùng Ubuntu24.04
- Đăng nhập với quyền `root` cho tất cả các bước cài đặt
- Các thao tác sửa file trong hướng dẫn này sử dụng lệnh nano hoặc vim
- Password thống nhất cho tất cả các dịch vụ là `Tien@123`
- Snapshot ngay sau PHASE 0
```

## III . DEMO CÁC BƯỚC

### 1. `PHASE 0` – Chuẩn bị nền (TẤT CẢ NODE)

- Static IP
- `/etc/hosts` đầy đủ
- Disable swap
- **Chrony** đồng bộ giờ
- `net.ipv4.ip_forward=1` (compute)
- `Bridge-nf-call-iptables`=1
- Nested KVM kiểm tra `OK`

### 2. `PHASE 1` – HA Infrastructure Layer

#### MariaDB Galera Cluster (controller1-3)

- Cài Galera trên cả 3 node

- Bootstrap cluster từ controller1

Kiểm tra:

```bash
SHOW STATUS LIKE 'wsrep_cluster_size';
#Phải = 3
```

=> nếu sai -> dừng lại sửa, không cài tiếp.

#### RabbitMQ Cluster (controller1-3)

- Join cluster đầy đủ

- Sync user openstack

Kiểm tra:

```bash
rabbitmqctl cluster_status
```

#### Memcached (controller1-3)

Đảm bảo listen đúng IP management.

#### HAProxy + Keepalived

VIP: `10.10.10.100`

Test:

```bash
ip a | grep 10.10.10.100
```

-> Failover thử bằng cách tắt 1 controller.

### 3. `PHASE 2` – Keystone (chỉ cài đầy đủ trên controller1 trước)

- Tạo DB keystone

- Cấu hình `keystone.conf` (trỏ DB về Galera)

Chạy:

```bash
keystone-manage db_sync
```

`Lưu ý`: Chỉ chạy trên controller1

- Bootstrap

- Tạo endpoint trỏ về VIP

- Sau khi `OK` → copy config sang controller2-3 → start service.

### 4. `PHASE 3` – Glance

1. Tạo DB

2. Cấu hình backend Ceph RBD

3. `db_sync` (chỉ controller1)

4. Start service cả 3 node

=> Test upload image.

### 5. `PHASE 4` – Placement

- Tạo `DB` → `db_sync` → register endpoint → test.

### 6. `PHASE 5` – Nova

#### Controller node

- `nova-api`
- `nova-scheduler`
- `nova-conductor`

Tạo DB:

- `nova_api`
- `nova`
- `nova_cell0`

Chạy:

```bash
nova-manage api_db sync
nova-manage cell_v2 map_cell0
nova-manage db sync
nova-manage cell_v2 create_cell
```

`Lưu ý`: Chỉ `controller1` chạy lệnh manage.

**Compute Node.**

- cài `nova-compute`
- cấu hình `transport_url` (`Rabbit cluster`)
- cấu hình `placement endpoint` (`VIP`)

Kiểm tra:

```bash
openstack compute service list
```

### 7. `PHASE 6` – Neutron (DVR mode)

Controller:

- `neutron-server`

Compute:

`neutron-openvswitch-agent`
`neutron-l3-agent (DVR)`
`neutron-metadata-agent`

`Nested` chú ý:

- MTU 1450
- Port security nếu dùng provider

#### 8. `PHASE 7` – Ceph + Cinder

Ceph phải healthy:

```bash
ceph -s
```

Tạo pool:

images
volumes
backups
vms

Cinder:

controller: `cinder-api` + scheduler  
storage: `cinder-volume` (RBD backend)

#### 9. `PHASE 8` – Horizon

Cài trên controller
Trỏ endpoint về `VIP`

## IV. DEPLOY

### `Bước 1`<`PHASE0`>: Tạo Volume + Network trên OpenStack hạ tầng

`Đối với tạo Volume`:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_03.png)

`Đối với tạo Network`:

- `Tạo dải mạng Tenant Network`: (Nhớ `MTU`=1450):

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_04.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_05.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_06.png)

- `Tạo dải mạng API + MNGT Network`:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_07.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_08.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_09.png)

- `Tạo dải mạng API + MNGT Network`:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_10.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_11.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_12.png)

Sau khi hoàn thành ta có 3 dải mạng sau:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_13.png)

`Đối với tạo Ports`:

- Vào Network rồi nhấp chuột trái vào dải mình muốn tạo Ports:(Nhớ add `Security Group`hoặc group Network mình tự tạo cho cụm)

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_15.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_16.png)
![OpenStack_HA_01](./images/Deploy_OpenStack_HA_17.png)

- Sau hoàn thành ta có các Ports được cấu hình:(Nhớ cấu hình chuẩn)

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_18.png)

=> Ta tạo Port với các dải còn lại

`Đối với tạo Instances`:(StorageNode nhớ dùng tool `Partition`, còn các Node khác dùng `Automatic`)

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_14.png)

=> Nếu thấy thừa IP có thể Detach Interface

`Đối với tạo Rules trên Security Group`- Các VM trong Project được gán vào Group `DEFAULT` lên ta Add Rule như sau:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_19.png)

### `Bước 2`: Cấu hình nền trên tất cả các Node (Controller,Compute,Storage Node)

#### 1. Cấu hình Interface

##### a. Trên Cotroller Node

Ta xoá đường route gateway `10.10.10.1` để có thể ssh vào được từ MobaXTerm:

```bash
ip route del default via 10.10.10.1 # Trên 3 controller node là SSH đc
```

Vào file `/etc/netplan`. Cấu hình **Ip static** cho từng interface trên từng Node Controller:

- Trên Controller 1:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0 đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

  GNU nano 7.2                                                       /etc/netplan/50-cloud-init.yaml *
network:
  version: 2
  ethernets:
    eth2:
      match:
        macaddress: "fa:16:3e:08:4f:e4"
      dhcp4: no
      addresses:
        - "192.168.70.72/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      nameservers:
        addresses: [8.8.8.8]
      set-name: "eth2"
    eth3:
      match:
        macaddress: "fa:16:3e:f2:f7:5f"
      addresses:
      - "10.10.10.11/24"
      set-name: "eth3"
      mtu: 1450

# Apply cấu hình

sudo netplan apply

```

- Trên Controller 2:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0 sai IP đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

network:
  version: 2
  ethernets:
    eth2:
      match:
        macaddress: "fa:16:3e:22:86:10"
      dhcp4: no
      addresses:
        - "192.168.70.74/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      set-name: "eth2"
      nameservers:
        addresses: [8.8.8.8]
    eth3:
      match:
        macaddress: "fa:16:3e:d8:5b:66"
      addresses:
      - "10.10.10.12/24"
      set-name: "eth3"
      mtu: 1450

# Apply cấu hình

sudo netplan apply
```

- Trên Controller 3:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0 sai IP đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

network:
  version: 2
  ethernets:
    eth2:
      match:
        macaddress: "fa:16:3e:b6:4b:46"
      dhcp4: no
      addresses:
        - "192.168.70.76/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      set-name: "eth2"
      nameservers:
        addresses: [8.8.8.8]
    eth3:
      match:
        macaddress: "fa:16:3e:24:5d:fe"
      addresses:
      - "10.10.10.13/24"
      set-name: "eth3"
      mtu: 1450

# Apply cấu hình

sudo netplan apply
```

`Lưu ý`: Địa chỉ `MAC` phải trùng khớp với Interface

##### b. Trên ComputeNode

Ta xoá đường route gateway `10.10.10.1` để có thể ssh vào được:

```bash
ip route del default via 10.10.10.1 # Trên 2 compute node là SSH đc
```

- Trên Compute Node 1:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0,2 sai IP đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

network:
  version: 2
  ethernets:
    eth3:
      match:
        macaddress: "fa:16:3e:72:c2:59"
      dhcp4: no
      addresses:
        - "192.168.70.78/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      set-name: "eth3"
      nameservers:
        addresses: [8.8.8.8]
    eth4:
      match:
        macaddress: "fa:16:3e:ee:e9:21"
      addresses:
      - "10.10.20.21/24"
      set-name: "eth4"
      mtu: 1450
    eth5:
      match:
        macaddress: "fa:16:3e:38:b3:00"
      addresses:
      - "10.10.10.14/24"
      set-name: "eth5"
      mtu: 1450

# Apply cấu hình

sudo netplan apply
```

- Trên Compute Node 2:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0,2 sai IP đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

network:
  version: 2
  ethernets:
    eth3:
      match:
        macaddress: "fa:16:3e:16:b1:45"
      dhcp4: no
      addresses:
        - "192.168.70.80/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      set-name: "eth3"
      nameservers:
        addresses: [8.8.8.8]
    eth4:
      match:
        macaddress: "fa:16:3e:6d:f9:28"
      addresses:
      - "10.10.20.22/24"
      set-name: "eth4"
      mtu: 1450
    eth5:
      match:
        macaddress: "fa:16:3e:d7:c2:83"
      addresses:
      - "10.10.10.15/24"
      set-name: "eth5"
      mtu: 1450

# Apply cấu hình

sudo netplan apply
```

##### c. Trên Storage Node

Ta xoá đường route gateway `10.10.10.1` để có thể ssh vào được:

```bash
ip route del default via 10.10.10.1 # Trên 3 storage node là SSH đc
```

- Trên Storage Node 1:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0,2 sai IP đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

network:
  version: 2
  ethernets:
    eth3:
      match:
        macaddress: "fa:16:3e:8a:89:22"
      dhcp4: no
      addresses:
        - "192.168.70.82/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      set-name: "eth3"
      nameservers:
        addresses: [8.8.8.8]
    eth4:
      match:
        macaddress: "fa:16:3e:0c:9a:20"
      addresses:
      - "10.10.10.16/24"
      set-name: "eth4"
      mtu: 1450
    eth5:
      match:
        macaddress: "fa:16:3e:14:85:ff"
      addresses:
      - "10.10.30.31/24"
      set-name: "eth5"
      mtu: 1450


# Apply cấu hình

sudo netplan apply
```

- Trên Storage Node 2:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0,2 sai IP đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

network:
  version: 2
  ethernets:
    eth3:
      match:
        macaddress: "fa:16:3e:07:06:cc"
      dhcp4: no
      addresses:
        - "192.168.70.84/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      set-name: "eth3"
      nameservers:
        addresses: [8.8.8.8]
    eth4:
      match:
        macaddress: "fa:16:3e:e9:ca:76"
      addresses:
      - "10.10.10.17/24"
      set-name: "eth4"
      mtu: 1450
    eth5:
      match:
        macaddress: "fa:16:3e:25:0d:8a"
      addresses:
      - "10.10.30.32/24"
      set-name: "eth5"
      mtu: 1450

# Apply cấu hình

sudo netplan apply
```

Trên Storage Node 3:

```bash
# Nano vào file netplan

sudo nano /etc/netplan/50-cloud-init.yaml

# xoá cấu hình eth 1,0,2 sai IP đi (Nếu có! Do 1 số cấu hình ip chèn thêm lúc ta tạo VM)

# Chèn hoặc sửa file chuẩn thành như sau:

network:
  version: 2
  ethernets:
    eth3:
      match:
        macaddress: "fa:16:3e:c9:f1:22"
      dhcp4: no
      addresses:
        - "192.168.70.86/24"
      routes:
        - to: "0.0.0.0/0"
          via: "192.168.70.1"
      set-name: "eth3"
      nameservers:
        addresses: [8.8.8.8]
    eth4:
      match:
        macaddress: "fa:16:3e:65:5f:6c"
      addresses:
      - "10.10.10.18/24"
      set-name: "eth4"
      mtu: 1450
    eth5:
      match:
        macaddress: "fa:16:3e:d8:cc:38"
      addresses:
      - "10.10.30.33/24"
      set-name: "eth5"
      mtu: 1450

# Apply cấu hình

sudo netplan apply
```

##### d. Kiểm tra

Trên cả 3 Node tiến hành ping ra ngoài mạng `google.com` và ra Gateway `192.168.70.1`

```bash
ping -c 4 192.168.70.1
ping -c 4 google.com
```

=> Đã kiểm tra trên lab các Node `OK` -> Chuyển bước tiếp

`Ví dụ`: Check trên Node Controller 1

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_20.png)

#### 2. Cấu hình hostname

Cấu hình đủ 2 config sau:

##### a. Cấu hình hostnamectl

Ví dụ - Trên Controller 1:

```bash
hostnamectl set-hostname controller1
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_21.png)

Làm tương tự với:

```text
controller1
controller2
controller3
compute1
compute2
storage1
storage2
storage3
```

##### b. Cấu hình file `/etc/hosts`

Cấu hình trên tất cả các Node:

```bash
# Chèn
10.10.10.11 controller1
10.10.10.12 controller2
10.10.10.13 controller3
10.10.10.14 compute1
10.10.10.15 compute2
10.10.10.16 storage1
10.10.10.17 storage2
10.10.10.18 storage3
10.10.10.100 vip
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_22.png)

##### c. Disable Swap

Cấu hình trên tất cả các Node:

```bash
swapoff -a
sed -i '/ swap / s/^/#/' /etc/fstab
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_23.png)

##### d. Cài gói cơ bản và Chorny

Cấu hình trên tất cả các Node:

```bash
apt update
apt install -y chrony vim curl wget net-tools
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_24.png)

##### e. Enable IP forward

Cấu hình trên 2 Node Compute + Controller Node:

```bash
echo "net.ipv4.ip_forward=1" >> /etc/sysctl.conf
echo "net.bridge.bridge-nf-call-iptables=1" >> /etc/sysctl.conf
modprobe br_netfilter
echo br_netfilter >> /etc/modules
sysctl -p
```

Trong đó:

- `net.ipv4.ip_forward=1`: Nó cho phép Linux forward packet giữa các interface.
- `net.bridge.bridge-nf-call-iptables=1`: Cho phép traffic đi qua bridge (OVS/Linux bridge) vẫn bị `iptables` xử lý.

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_25.png)

##### f. Kiểm tra Nested KVM

Check trên Compute Node:

```bash
egrep -c '(vmx|svm)' /proc/cpuinfo
```

=> Nếu kết quả `>0` là `OKE`.(Số càng to càng tốt nhé là số core hỗ trợ ảo hoá)

##### g. Tắt Firewall

Trên tất cả các node cấu hình:

```bash
systemctl disable ufw
systemctl stop ufw
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_26.png)

### `Bước 3`: Snapshot Volume để tiện Rollback khi có sự cố

#### 1. Tắt tất cả VM trên OpenStack hạ tầng

Nên shutdown VM trước để sạch:

```bash
shutdown -h now
```

Hoặc từ Horizon: `Shutdown instance`.

#### 2. Create Snapshot Volume

Do VM trong Bài lab ta boot from Volume lên ta sẽ `snapshot` volume là chuẩn nhất (`snapshot` VM nếu VM boot từ ephemeral disk)

Và do dung lượng Volume trên hạ tầng OpenStack có hạn lên ta sẽ snapshot 3 Controller Node vì đang cần cấu hình DB và Rabbit MQ chủ yếu config controller thui

Vào `Projects`->`Volumes`->`Volumes`->`Create SnapShot`, ta sẽ được 3 Volume SnapShot:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_27.png)

Khi cần Rollback ta chỉ cần:

`Bước 1`: Stop VM

```bash
Project → Compute → Instances → Stop Instance
```

`Bước 2`: Vào Volume Snapshot

```bash
Project → Volumes → Snapshots
```

Chọn `snapshot` → **Create Volume**

`Bước 3`: Detach volume cũ

```bash
Project → Volumes → Volumes
```

→ Detach Volume khỏi instance

`Bước 4`: Attach volume mới (tạo từ snapshot)

Attach volume mới vào VM.

`Bước 5`: Start lại VM

### `Bước 4`:<`PHASE1`>Cài và Config Maria DB + Galera HA

Trên cả 3 Node Controller:

```bash
apt update
apt install mariadb-server galera-4 -y
```

Check:

```bash
systemctl status mariadb.service
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_28.png)

Tắt service trước khi config trên 3 controller:

```bash
systemctl stop mariadb
```

Cấu hình Galera:

- Trên cả 3 Controller chỉnh file:

```bash
nano /etc/mysql/mariadb.conf.d/60-galera.cnf

# Dán config

[mysqld]
bind-address=0.0.0.0
binlog_format=row
default_storage_engine=InnoDB
innodb_autoinc_lock_mode=2

# Quan trọng (khai báo wsrep_provider)
wsrep_provider=/usr/lib/galera/libgalera_smm.so

[galera]
wsrep_on=ON
wsrep_cluster_name="openstack_cluster"
wsrep_cluster_address="gcomm://10.10.10.11,10.10.10.12,10.10.10.13"

wsrep_node_name="controller1"
wsrep_node_address="10.10.10.11"

# Đối với controller 2. Thay 2 dòng

wsrep_node_name="controller2"
wsrep_node_address="10.10.10.12"

# Đối với controller 3. Thay 2 dòng

wsrep_node_name="controller3"
wsrep_node_address="10.10.10.13"
```

Tiếp theo, ta sẽ thử Bootstrap cluster trên Controller 1

```bash
galera_new_cluster
```

Check:

```bash
mysql -u root -e "SHOW STATUS LIKE 'wsrep_cluster_size';"
mysql -u root -e "SHOW STATUS LIKE 'wsrep_cluster_status';"
```

=> Ra `1` và `Primary` là Node Controller 1 đã Bootstrap thành công

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_29.png)

Giờ để có mô hình Galera 3 Node HA thành công ta cần phải Boot tiếp Cluster Galera từ 2 Node còn lại :

- Khởi tạo Service trên Controller 2:

```bash
systemctl start mariadb
```

- Check trên Controller 1:

```bash
mysql -u root -e "SHOW STATUS LIKE 'wsrep_cluster_size';"
```

=> Ra `2` là thành công tạo thêm cluster mới

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_30.png)

- Khởi tạo Service trên con Controller 3:

```bash
systemctl start mariadb
```

- Check trên Controller 1:

```bash
mysql -u root -e "SHOW STATUS LIKE 'wsrep_cluster_size';"
```

=> Ra `3` là thành công tạo thêm cluster mới

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_31.png)

Như vậy ta đã có database HA 3 node. Giờ chỉ còn Set passwork remote access với user `Root`.

- Trên 1 Controller bất kì:(Controller 1)

```bash
mysql_secure_installation

# Sau đó nó sẽ hỏi:

1. Có muốn set pass cho tài khoản root ? <Answer:Tuỳ>
2. Có muốn set xác thực lớp unix_socket ? <Answer:Tuỳ>
3. Có muốn bỏ user ẩn danh ? <Answer:Có>
4. Có cho truy cập remote qua mạng bằng user root không? <Answer:Có>
5. Có muốn bỏ database test không ? <Answer:Có>
6. Có muốn Reload bảng quyền ngay lập tức không? <Answer:Có>

# Sau đó đăng nhập vào mysql

mysql -u root -p

# Tạo user root remote accesss(tk root truy cập DB từ xa, bỏ nếu ae set ở trên rồi!)

CREATE USER 'root'@'%' IDENTIFIED BY 'Tien@123';
GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' WITH GRANT OPTION;
FLUSH PRIVILEGES;

# Check

SELECT user, host FROM mysql.user;
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_32.png)

Test login Remote từ Controller khác:(Controller 2)

```bash
mysql -u root -p -h 10.10.10.11

# -u: Đăng nhập với user root
# -p: Yêu cầu nhập Password
# -h: Kết nối MySQL ở IP 10.10.10.11(Nếu không nó sẽ kết nối tới localhost -> Dùng socket nội bộ)
```

Kết quả:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_33.png)

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_34.png)

Test lại Galera còn Primary và Size bằng 3 không(Trên cả 3 controller):

```bash
SHOW STATUS LIKE 'wsrep_cluster_status';
SHOW STATUS LIKE 'wsrep_cluster_size';
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_35.png)

`LƯU Ý QUAN TRỌNG`: **Không được** `Shutdown` **các VM cùng 1 lúc**. Sau khi `Shutdown` or `Restart` lại các Controller thì ta lên check trước 3 Node Controller:

```bash
cat /var/lib/mysql/grastate.dat

# Nếu Node nào hiện

safe_to_bootstrap: 1

# Thì phải khởi động Node đó đầu tiên bằng

galera_new_cluster

# Không cả 3 cluster sẽ chết 

# Bản chất Galera chỉ đặt safe_to_bootstrap: 1 cho node cuối cùng shutdown đúng cách.

# Nếu Cluster đang healthy or restart từng node một thì safe_to_bootstrap: 0 => Thì OK không cần dùng lệnh trên

# Sau đó restart mariaDB ở 2 node còn lại
systemctl start mariadb
```

Do cấu hình Galera HA đã ổn lên ta sẽ snapshot volume hiện tại và xoá volume trước đó đồng thời test `safe_to_bootstrap: 1` luôn

- Tắt hết 3 Controller VM và xoá Volume snapshot cũ và Add một cái mới:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_36.png)

- Khởi động lại và ta sẽ Check xem Node Nào sẽ phải Bootstrap lại đầu tiên:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_37.png)

=> Ta thấy ở đây Node Controller 3 đang có `safe_to_bootstrap: 1` lên ta phải bootstrap lại từ nó trước

- Check lại `wsrep_cluster_status` và `wsrep_cluster_size`:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_38.png)

=> Size `=1` Start lại MariaDB service trên 2 Node còn lại:

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_39.png)

=> Size `=3` Là Oke và giờ thằng `10.10.10.13` là thằng primary DB, nhớ remote access DB vào nó !

### `Bước 5`: Cài và Config Rabbit MQ Cluster (Message Queue HA)

Đầu tiên, cài Rabbit MQ trên cả 3 Controller:

```bash
apt update
apt install rabbitmq-server -y
```

Sau đó enable service:

```bash
systemctl enable rabbitmq-server
systemctl start rabbitmq-server
```

Check:

```bash
systemctl status rabbitmq-server.service
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_40.png)

Sau đó, đồng bộ **Erlang Cookie**

- Trên Controller 1:

```bash
cat /var/lib/rabbitmq/.erlang.cookie
#Copy nội dung hiện ra
```

- Trên Controller Node 2 + 3:

```bash
systemctl stop rabbitmq-server

# Edit file:

nano /var/lib/rabbitmq/.erlang.cookie

# Paste nội dung giống hệt controller 1 (Nhớ phải chuẩn không thừa khoảng trắng cuối dòng)

# Rồi sau đó set quyền cho file trên 3 controller
chown rabbitmq:rabbitmq /var/lib/rabbitmq/.erlang.cookie
chmod 400 /var/lib/rabbitmq/.erlang.cookie
```

Bước tiếp đó ta sẽ **Join Cluster**

- Trên Controller 2 + 3:

```bash
systemctl start rabbitmq-server
rabbitmqctl stop_app
rabbitmqctl reset
rabbitmqctl join_cluster rabbit@controller1
rabbitmqctl start_app
```

Sau đó ta sẽ kiểm tra Cluster

- Trên bất kì controller nào:

```bash
rabbitmqctl cluster_status

# Phải thấy

{running_nodes,[rabbit@controller1,
                rabbit@controller2,
                rabbit@controller3]}
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_41.png)

Tiếp theo ta tạo user cho OpenStack:(Trên Node Controller 1)

```bash
rabbitmqctl add_user openstack Tien@123
rabbitmqctl set_permissions openstack ".*" ".*" ".*"
# Form set_permissions <user> <conf> <write> <read>
# ".*" là trao tất cả quyền
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_42.png)

### `Bước 6`: Cài memcached

Trên 3 Node Controller cùng config như sau :

```bash
# Cài memcached

apt install memcached python3-memcache -y

# Sửa file
nano /etc/memcached.conf

# Tìm dòng
-l 127.0.0.1

# Sửa thành
-l 0.0.0.0

# Restart
systemctl restart memcached
systemctl enable memcached
```

=> Sau bước này ta lại làm mới volume snapshot để chuyển qua bước tiếp

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_43.png)

### `Bước 7`: Cài + Config Keepalived + HA Proxy

`Lưu ý`: Nhớ cái cấu hình `safe_to_bootstrap=1` sau mỗi lần snapshot

Trên 3 Node Controller ta cấu hình kiến trúc HaProxy với đường VIP để đáp ứng điều kiện sau:

- Cân bằng tải API giữa 3 controller
- Frontend 1 VIP (VD: 10.10.10.100)
- Backend: controller1, controller2, controller3
- Tất cả Service OpenStack sẽ cấu hình vào VIP này thay vì từng Controller
- `Keepalived` **tạo và quản lí** `VIP`:

  - 1 Node Master
  - 2 Node Backup
- `HAProxy` làm LoadBalance cho các services (MariaDB, Keystone, RabbitMQ…)

Đầu tiên, ta cài `keepalived`+ `HAProxy`:

- Trên cả 3 node Controller:

```bash
apt install keepalived -y
apt install haproxy -y
```

- Enable `keepalived`+ `HAProxy`:

```bash
systemctl enable keepalived
systemctl enable haproxy
```

![OpenStack_HA_01](./images/Deploy_OpenStack_HA_44.png)

Tiếp theo, ta sẽ cấu hình `HA Proxy`: (Trên cả 3 Node Controller)

- Đầu tiên, tất cả IP thật đều bind vào MariaDB hoặc IP máy local đang bind chính nó (`0.0.0.0:3306`/`127.0.0.1:3306`). Ta phải config để HAProxy có thể bind được `VIP:3306`. Vậy nên trên 3 Controller:

```bash
# Sửa file
nano /etc/mysql/mariadb.conf.d/50-server.cnf

# TÌm dòng
0.0.0.0

#Hoặc
127.0.0.1

# Sửa thành
10.10.10.11    # Đối Controller1
10.10.10.12    # Đối Controller2
10.10.10.13    # Đối Controller3

# Đừng có ghi cả 3 IP mà Controller nào mới chỉnh IP đấy

# Rồi restart lại service
systemctl restart mariadb
```

```bash
# Sửa file
nano /etc/haproxy/haproxy.cfg

# Xoá config cũ và thêm phần này vào cuối file:
global
    log /dev/log local0
    maxconn 4096
    daemon

defaults
    log global
    mode tcp
    option tcplog
    timeout connect 10s
    timeout client  1m
    timeout server  1m

# MariaDB
frontend galera_front
    bind *:3306
    default_backend galera_back

backend galera_back
    balance roundrobin
    option tcp-check
    server controller1 10.10.10.11:3306 check
    server controller2 10.10.10.12:3306 check
    server controller3 10.10.10.13:3306 check

# RabbitMQ
frontend rabbit_front
    bind *:5672
    default_backend rabbit_back

backend rabbit_back
    balance roundrobin
    option tcp-check
    server controller1 10.10.10.11:5672 check
    server controller2 10.10.10.12:5672 check
    server controller3 10.10.10.13:5672 check

# Sau đó restart để ăn cfg:
systemctl restart haproxy
```

Rồi sau đó, ta cấu hình `keepalived`

- Cấu hình Controller 1 (`MASTER NODE`):

```bash
nano /etc/keepalived/keepalived.conf

# Cấu hình
vrrp_instance VN_1 {        # Tạo 1 instance vrrp có tên VN_1
    state MASTER            # Node Master để giữ VIP
    interface eth3          # VIP gắn vào interface này
    virtual_router_id 51    # ID cụm vrrp
    priority 150            # Độ ưu tiên (cao nhất là node master)
    advert_int 1            # Time gửi gói vrrp
    authentication {        # Cơ chế bảo mật giữa các node
        auth_type PASS      # Kiểu xác thực Pass
        auth_pass Tien@123  # Mật khẩu dùng giữa các node
    }
    virtual_ipaddress {     # Đây là VIP
        10.10.10.100        # Keepalived tự add/remove khi failover
    }
}
```

- Cấu hình Controller 2,3 (`BACK NODE`)

```bash
# Đối với Controller 2 đổi 2 dòng sau và giữ nguyên all
state BACKUP
priority 120

# Đối với Controller 3 đổi 2 dòng sau và giữ nguyên all
state BACKUP
priority 100
```